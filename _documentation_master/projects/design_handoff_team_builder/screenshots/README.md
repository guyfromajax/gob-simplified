# Screenshots

Reference captures at zoom 1. The `Prototype view` bar at the top of each is review scaffolding and is not part of the design.

| File | Screen |
| --- | --- |
| `1-program-select.png` | Program Select — browse mode. Header, Team Builder entry band, filter bar, and the first three of sixteen conference sections. The remaining conferences were hidden for capture only; the real page runs all 16. |
| `2-identity-studio.png` | Chapter Ⅱ · Identity — control rail left, banner and jersey previews top-right, court below. All artwork drawn by the shipped generators. |
| `3-build-mode-gate.png` | Build mode gate — two cards, nothing preselected, `Continue to Roster` disabled until one is chosen. |
| `4-roster.png` | Chapter Ⅲ · Roster — state strip, budget band with the legality verdict, the board of fifteen, and the inspector. |
| `5-review.png` | Review — hero banner, the fifteen, eligibility + standings left, Team Measures + Program Details right, Home Court below. |
| `6-establish.png` | Establish sequence at its final beat: charter written, the replaced program's standings row swapped for the new one, `Enter Franchise` revealed. |
| `7-banner-options.png` | The five banner compositions side by side. Switch the test palette to **Pale** in the live file to see why the chevron is being retired. |

**Not captured, worth opening live:**
- **Claim in Team Builder mode** — click *Open Team Builder* on the Claim screen. Five things change at once (banner, headline, subhead, entry band, selection bar); a static image only shows one of them.
- **The illegal roster state** — move any attribute slider to break a budget. The verdict cell turns red with the specific reason and a *Take me there* jump, and the primary action disables.
- **The establish sequence in motion** — the three beats are the design. There is a **Replay** button at the bottom-right.
