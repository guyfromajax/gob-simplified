# Documentation Sweep — Checklist & Work Plan

**Created:** 2026-06-12
**Goal:** Review every file in `_documentation_master/` to confirm it is current, flag files to delete or combine to avoid doc bloat, and identify docs needing significant updates.

**SCOPE (recalibrated 2026-06-13):** Review the **system-doc folders only**, through `07_Design_Systems`. **In scope:** `00_*` → `07_Design_Systems`. **Out of scope (not reviewing):** root standalone files (`ENV_VARIABLES.md`, `SECURITY_BASELINE.md`), `tasks/`, all of `projects/` (incl. `projects/Website_Copy/`, `projects/secondary_announce/`, `projects/Z-Completed/`), and `projects/scouting-tutorial-reference-files/`. After `06_GMO_Supporting_Systems` + `07_Design_Systems` are done, the sweep is **complete**. ✅ **SWEEP COMPLETE 2026-06-13** — all in-scope folders reviewed (117/117).

## How to use

- Mark `[x]` when a file has been reviewed.
- After each file, record a verdict after the `→`:
  - **Keep** — current and accurate, no action needed
  - **Update** — needs significant updates (note what)
  - **Combine** — fold into another doc (note the target)
  - **Delete** — obsolete / superseded
- Add brief notes inline as needed.

**Sunset modes decision (2026-06-12):** Single Game mode and Tournament mode are sunset. Their docs get **deleted** as we reach them, replaced by a single one-page sunset note (tag + key learnings) to be drafted when we hit `01_Game_Mode_Systems`. Code removal is a separate future project (tag commit first, staged deletion). Live docs that merely mention these modes get a "(sunset)" tag, removed when the code goes.

**Progress: 117 / 117 in-scope reviewed — SWEEP COMPLETE (2026-06-13).** 4 merged+deleted along the way (Court_Template_Spec.md + Play_Builder_System.md + Playbook_Likely_Shot_Percentage_System.md + Training_Notes_System.md, so in-scope total dropped 121→117). All folders `00_*` → `07_Design_Systems` are done. The two `07_Design_Systems` `.html` design-prototype bundles were **deleted 2026-06-13** (their FTE v2 features shipped; live implementation is now the reference, `fte_system.md` link repointed).

---

## 00_Data_Systems (3)

- [x] `00_Data_Systems/Database_System.md` → **Updated 2026-06-12** — rewritten as top-level DB doc: full 21-collection inventory from `db.py`, identity conventions (incl. FTD ObjectId vs FPD/FRD string `franchise_id`), startup indexes, env/db selection. Play-storage detail now lives only in `O_&_D_Plays_Collections.md`.
- [x] `00_Data_Systems/Games_Collection.md` → **Updated 2026-06-12** — trimmed 218→~70 lines. Fixed stale structure claims (`home_team`/`away_team` dicts no longer exist; `text_log` not persisted); documented legacy `team1_id`/`team2_id` result fields (still written by save-result, contrary to old doc); dropped Jan-2025 changelog narratives, stale line numbers, and "Future Documentation" placeholder; cleanup section rewritten as implemented behavior (single-game delete endpoint, franchise/tournament cascade deletes).
- [x] `00_Data_Systems/O_&_D_Plays_Collections.md` → **Updated 2026-06-12** — content verified current against code + seed scripts (play copy shape, `defenses` schema, normalize contract, play_id-first persistence, set-play alias remap). Fixed stale Related Docs links; added `motion_focus` to play-copy example. Canonical play/defense storage doc; `Database_System.md` now points here.

## 00_General_Systems (34)

- [x] `00_General_Systems/Account_Modal_System.md` → **Moved 2026-06-12** to `projects/Account_Modal_Player_View_Brief.md` — it's a feature brief, not system docs. Sections 1–2 (Username, Scouting Ambience) verified live; Section 3 (Player View sprite toggle) is planned/unbuilt. Added status header.
- [x] `00_General_Systems/Active_Page_Analysis.md` → **Deleted 2026-06-12** — Spring-2026 point-in-time navigation audit, already stale (predates news/account pages; lists sunset tournament pages as active). Its orphaned-page hit list is recoverable from git history when the frontend purge project starts.
- [x] `00_General_Systems/Box_Score_System.md` → **Rewritten 2026-06-12** — converted from briefing doc to full system documentation. Now traces the three production paths (user/engine games, full CPU sims, distant statistical generation), game-doc persistence shapes, EOG sequence (phase A/B, `finalize_game` FPD rollup, `eog_inputs` → `team_attribute_changes`), and display surfaces.
- [x] `00_General_Systems/COURT_REDESIGN_HANDOFF.md` → **Deleted 2026-06-12** — completed implementation brief; redesign verified live in `court.html` / `gameScene.js` (playcall strip, momentum bars, scoreboard rank/record, timeout pips, shot-clock critical pulse, vibrant-rgb vars all present). Recoverable from git history.
- [x] `00_General_Systems/Coaching_Archetype_System.md` → **Verified + code fix 2026-06-12** — doc accurate (classifier, schema, stash/commit, bulk-sim, endpoints, surfaces all match code). Review surfaced a real bug: evolution detection (`record_archetype_change_if_any`) was only wired to the orphaned `/franchise/save-result` endpoint; moved it into the live phase-A user-game block. Doc "Detect" wording synced.
- [x] `00_General_Systems/Coordinate_Orientation_Audit.md` → **Deleted 2026-06-12** — point-in-time working audit for the shot-location/opposite-side-defender bug, since resolved the *opposite* way from its recommendation (`player.coords` now stores display orientation; HOME-orientation convention sunset via neutralized `_normalize_animation_coords_to_runtime_home`). Actively misleading if read today. Note: add a "runtime coords convention" line to `Defense_Coords_System.md` when reviewed.
- [x] `00_General_Systems/DESIGN_HANDOFF.md` → **Deleted 2026-06-12** — completed shell-redesign implementation brief; verified live (`#0b0d14` body bg across 20+ page CSS files, new shell `::before` tokens in `resource-pages.css`, inbox badge + pulse in `franchise-command-center.css`). Recoverable from git history.
- [x] `00_General_Systems/Defense_Coords_System.md` → **Verified + updated 2026-06-12** — core content accurate to code (unified `get_defender_coords`/`calculate_defender_coords` contract, zone overlap/offset logic, fallback-path HOME-orientation fix all match `shared_defense.py`). Fixed stale bits: zone constants live in `shared_defense.py` not `constants.py`; removed drifted line numbers; removed dead `DEFENDER_COORDINATE_SYSTEM_REFACTORING_PLAN.md` reference; added runtime-coords-convention note (display orientation, per Coordinate_Orientation_Audit deletion) and a short HCT zone-defense section pointing to `FCP_HCT_System.md`.
- [x] `00_General_Systems/Deploy_To_Live_System.md` → **Verified, no changes 2026-06-12** — operational runbook fully accurate: `maintenance.json` schema, `_redirects` wildcard pattern, `maintenance.html`, backend `MAINTENANCE_MODE` middleware (503 on writes, `/health` exempt), and `maintenanceBanner.js` behavior (localStorage dismissal by id, deferred game pages, America/New_York naive-time parsing) all match code.
- [x] `01_UX_Systems/Font_Color_System.md` (moved from 00_General_Systems post-review) → **Rewritten 2026-06-12** — core premise ("identical colors everywhere") no longer true: thresholds are unified (90/80/70) but set-lineup surfaces moved to the bright redesign palette (`#34EC27`/`#F5C518`/`#ff9f43`/`#ff6d6d`) while court box score + player cards kept the dark palette (`#00aa00`/`#cccc00`/`#ff8800`/`#cc0000`). Roster grid no longer uses background colors (now class-based font color on NG cell). Added the Player View card border surface; dropped drifted line numbers.
- [x] `00_General_Systems/Front_End_Architecture.md` → **Verified + minor fixes 2026-06-12** — court-page `#app-grid` architecture accurate (grid areas, invariants, `updatePlaycallPosition` legacy guard, `grid-column: 2` playcall constraint all match `court.html`). Fixed drifted values: row-3 clamp now `clamp(140px, 20vh, 240px)`, `--scoreboard-height` now 120px; repointed dead `playcall_center_width_constraint.md` reference to `playcall_center_future_work.md`.
- [x] `00_General_Systems/GOB FCC Shell POC.html` → **Deleted 2026-06-12** — visual spec POC for the completed shell redesign (referenced only by the deleted `DESIGN_HANDOFF.md`).
- [x] `00_General_Systems/GOB Mode Select POC.html` → **Deleted 2026-06-12** — same as above.
- [x] `00_General_Systems/GOB Set Lineup POC.html` → **Deleted 2026-06-12** — same as above.
- [x] `00_General_Systems/Geek_Points_System.md` → **Verified + one addition 2026-06-12** — accurate: all win/loss point ranges, EOS phase/round ranges, bulk-sim 2x policy, lazy `geek_points_by_team` creation, owner-only crediting, and both leaderboard endpoints match `franchise_geek_points.py` / `leaderboard_routes.py`. Added the `?view=rank` option on `/api/leaderboard/by-team`; fixed a stale doc-path reference in the code module's docstring.
- [x] `01_UX_Systems/Loading_Overlay_System.md` (path corrected) → **Updated 2026-06-12** — core facts held (shared `PageLoadOverlay` API, `loader1.gif`, FCC `hideFccLoadingOverlay`), but the module grew a second "pulse" variant (team banner + title + rotating stat feed via `buildPostgameStatFeed`/`updatePulseSubtitle`) and several new consumer surfaces (training, box-score, team-select, EOG transition, accessDenied) — all added. TCC entry marked sunset; stale `docs/docs_1_systems/` paths fixed. Overlap check done 2026-06-12: no redundancy with `Page_Load_System.md` (fetch/cache pattern) — distinct topics; `UX_Page_Load_System.md` deleted.
- [x] `00_General_Systems/Manual_QA_Checklist.md` → **Rewritten 2026-06-12** — two of its three mode sections tested sunset modes (Single Game, Tournament); removed per the sunset decision. Replaced with current product coverage: FTE tutorial funnel smoke test (new section) + expanded Franchise weekly-loop checks (complete-week, recruiting/training, news, geek points, season edges). Dropped stale "Bug #9" references (quarter-break verification folded into the franchise flow); fixed stale location header.
- [x] `00_General_Systems/New_Team_Creation_System.md` → **Combined + deleted 2026-06-12** — content verified accurate, then folded into `00_Data_Systems/Database_System.md` as the "Adding New Teams & Players (manual seeding)" section (it already pointed there for the doc shapes; Mode_Init was considered as a home but rejected — that doc covers runtime init, not seed-data authoring). Standalone file deleted; no other cross-references existed.
- [x] `01_UX_Systems/Page_Load_System.md` (moved from 00_General_Systems post-review) → **Verified + updated 2026-06-12** — the scoped-fetch + sessionStorage pattern is real and matches code: `js/shared/resourceCache.js` implements the exact documented key pattern (`resource:{page}:{fid}:{season}:{week}:{scope}`); standings/stats/team-traits/rankings behave as documented. Fixed: Schedule diverged (now one national payload via `/franchise/schedule/national`, no ResourceCache) — section rewritten; added two new consumers (`leaders-v2`, `fcc-team-stats`) and named the shared module. Distinct concern from `Loading_Overlay_System.md` (data fetch/cache vs. visual overlay) — no combine.
- [x] `00_General_Systems/Plays_Page_System.md` → **Verified + updated 2026-06-12** — accurate: `play_id`-preferred fetch with `play_name` fallback matches `play-details.html` (`loadPlayData`), both routes exist in `play_routes.py`, skeleton rules (motion `base_loop`, set `successful` w/ steps or versions) match. Added a data-source note the doc omitted: both routes read the **`gob-staging`** `plays` collection (builder workflow), not the active DB. Fixed stale `docs/docs_1_systems/` related-doc paths.
- [x] `00_General_Systems/Position_Checkpoints_and_Snapshot_Schema.md` → **Verified + updated 2026-06-12** — implementation reference accurate: all 12 ledger helpers, `SCHEMA_VERSION=1`, wiring sites (7 files / 41 refs, matches §12.4), tests + `scripts/audit_position_snapshots.py` all exist (audit script runs clean). Updated: fast break now has a **third** resolver (`after_steal_fast_break.py`) — §6.2 two-path table corrected to three paths, and a new §8.2 known-gap row added since the new resolver attaches **no** position snapshots. Re-verified the FB `DEFENSIVE_STOP` gap is still open.
- [x] `00_General_Systems/Resend_System.md` → **Verified + updated 2026-06-12** — config vars, defaults, failure handling, and HTML escaping all match `resend_sender.py` / `alpha_access_email.py`. Added two missing flows (alpha waitlist email, 12-question alpha survey email) and a "Not Resend" pointer: password resets use **SendGrid** via separate `email_sender.py`.
- [x] `00_General_Systems/Sound_Design_System.md` → **Verified + minor additions 2026-06-12** — trigger→SFX inventory holds up on spot checks (homepage CTA, mode-select, set-lineup autoset, training coaching styles, gameSfx pools + `DEBUG_GAME_SFX`); all referenced assets exist in `sounds/`, `_SFX_notes.md` present. Added the missing shot-clock-violation → whistle-3 row; marked Mode-Select scrimmage/tournament rows as sunset-mode wiring. **Boundary cleanup (2026-06-12, during SFX_System review):** rescoped to UI-interaction sounds only — cut the 3 intro paragraphs duplicating `SFX_System.md`'s Runtime section (drift channel), moved the timeout-airhorn + foul/turnover/shot-clock whistle rows to `05_Features/SFX_System.md`, demoted `_SFX_notes.md` from "canonical spec" to original wiring work order (it's a task list with WORKING/NOT-WORKING flags). Clear split now: Sound_Design = user-interaction sounds; SFX_System = simulation-driven audio.
- [x] `00_General_Systems/Statistics_System.md` → **Verified + updated 2026-06-12** — spot checks all pass (PIP fast-break gate `pip_stat_eligible`, FB_PTS, 90/10 putback split, 50% SCR_S roll, per-play `fast_break_plays` incl. `after_steal`, `BOX_SCORE_KEYS`). Added missing `POT` (points off turnovers) stat + an After Steal resolver stat-path note. **Code fix applied 2026-06-12:** `after_steal_fast_break.py` was recording PIP on FB makes and skipping FB_PTS/POT — now records FB_PTS + POT (no PIP), matching policy. Remaining gap (noted, not fixed) `[CODE-CLEANUP]`: the After Steal short-circuit skips the legacy team `Fast_Break_Success`/`vs_Fast_Break.success` and defender `FB_A_D`/`FB_S_D` bookkeeping. Absorbed the `06_Features` supplement (play_id rollups, training-report delta keys, canonical-rebounder fix).
- [x] `00_General_Systems/Step_By_Step_System.md` → **Verified + updated 2026-06-12** — core step inventory accurate on spot checks (Handoff/Kickout/Walk Up orchestrator conditions incl. x<71/>29 thresholds, FT emitter + `FREE_THROW_SHOT_GRID_PER_GAME_SECOND=12`, CR defensive-stop formula — code cites this doc, RR Stage B/D/E decision logic, universal FB geometry flags). Fixed: After Steal is now schema-rendered via its own `after_steal_fast_break_step_emitter.py` (doc claimed "still on legacy fastBreak.js"); added an After Steal emitter section; corrected the RR outlet-contest skill check (broken table + wrong formula — code uses a 10-grid distance gate and `1.5×off + 3×fb_eff > def + 2×fb_opp`, team attrs not in the bases); synced stale Stage 1 formula (old shared-die version) with the live separate-die + team-attr formula; filled the empty "Final Shot" stub; dropped drifted line numbers (`skeleton_step_emitter.py:1988`, turn_manager `~1671–1687` → `run_micro_turn`).
- [x] `00_General_Systems/Styleguide_updated.md` → **Verified + absorbed old styleguide + renamed 2026-06-12** — now the single canonical style guide at `00_General_Systems/Styleguide.md` (renamed after the superseded `07_Design_Systems/Styleguide.md` was deleted; all live references repointed). Spot checks pass against live CSS (FCC tab parallelogram clip-path/sizing, `#0b0d14` shell, button system, modal classes). Absorbed the three sections unique to the superseded `07_Design_Systems/Styleguide.md`: §Semantic/Data Colors, §Tutorial Alert (Coach Card), §Tutorial Alert Resume Footer. Fixed drift: Recruit RT breakpoints now apply to **JH recruits only** (FR/SO/JR use the player scale via `getRecruitRtBucketClassForYear` — post recruit-year update); modal-type intro corrected from "two types" to three + sub-patterns; shared-rules heading generalized.
- [x] `00_General_Systems/Team_Images_System.md` → **Verified + updated 2026-06-12** — core mechanics all match code: `nameToTeamSlug` / `TEAM_ASSET_SPEC` / `getTeamAssetPath` in `common.js` (4 asset keys, documented extensions, general fallback), `images/teams/general/` has all four assets, `teams/128_teams.txt` exists. Refreshed the stale usage table: added newer banner consumers (mode-select franchise card, box-score header, EOG completion popup/stats panel, pulse loading overlay, FTE tutorial situation, press-conference logo) and tagged single-game/tournament/TCC surfaces as sunset.
- [x] `00_General_Systems/UESS_Backlog.md` → **Keep + status header added 2026-06-12; moved to `projects/UESS_Backlog.md`** (work-tracking doc, not a system doc; now the single source of truth for remediation statuses — `UESS_System.md` §12 trimmed to a pointer; all cross-references repointed) — archived May-2026 violation catalog, intentionally point-in-time (file:line citations left as-is). Re-checked all 17 remediation items against code and added a dated status block: items 1 (§8.1 step-chain), 2 (handoff drift→setup_coords), 4 (FB post-shot sub-steps), 11 (After-Steal migration) are DONE; 3 largely done (DREB announcements, `schema_rendered_arc` now correct); 6 partial (FT make-hold). Still open: 5, 7, 8, 9 (`shot_state_snapshot` still 0 hits), 10 (Opening Tip), 12–17. Obsolete After-Steal rows (§2.7, §3.2) marked resolved inline.
- [x] `00_General_Systems/UESS_System.md` → **Verified + updated 2026-06-12** — contract spec largely accurate: schema shape, closed vocabs (PlayerAction list, archetype rates 13/14/14/18/32), ball-motion constants (24/40/30/27/12), clock-ledger wiring, coords tracking, overlay invariants all match code. Fixed: §2 migration table had Opening Tip wrong in both directions (marked Migrated but emitter never built → Not migrated) and Final Shot wrong (marked Not migrated but routes through `_emit_hco_animation_steps` → Migrated); §6 ownership contract rewritten from "Active" to partially built (code stamps `uess_ownership_contract` blob on subset of turns; spec'd discrete fields unbuilt); §7 shot snapshot re-labeled from "Active" to not-yet-implemented build target; §12 trimmed to a pointer at `projects/UESS_Backlog.md` (single source of truth for statuses) plus a two-line headline state; fixed FT-row copy/paste note in §11.1.
- [x] `00_General_Systems/UX_Page_Load_System.md` → **Deleted 2026-06-12** — design-direction doc for a hybrid FCC shell model (in-shell summary tabs + routed pages for heavy destinations) that was never implemented: today's FCC runs all 14 destinations as local in-shell tabs with no tab routing. User confirmed the hybrid restructure is not planned anytime soon. Its caching/progressive-disclosure rules already live in `Page_Load_System.md` (near-verbatim duplicate). Stale reference in `01_UX_Systems/Loading_Overlay_System.md` repointed to `Page_Load_System.md`.
- [x] `00_General_Systems/Unified_Animation_System.md` → **Keep + status header; moved to `projects/Unified_Animation_System.md` 2026-06-12** — March-2026 draft working spec for the FE execution-unit contract / telemetry layer (hybrid-authority migration regime). Still live code: `unitCompletionContract.js` imported by 6 animation modules; `HCO_STEP_MOVEMENT_STRICT_CONTRACT` flag + `hco_uess_elapsed_observe` / FB telemetry wired. Intentionally non-compliant with the UESS pure-renderer rule (it governs the violation period; retired when UESS backlog item 13 closes) — status header makes that explicit. Fixed stale `docs/docs_1_systems/` UESS path; copied the two P0 HCO clock-overrun issues into `bugs.md`; repointed `Turn_by_Turn_System.md` references.
- [x] `00_General_Systems/User_Account_System.md` → **Updated 2026-06-12** — core auth/JWT/OTP/password-reset/rate-limit content verified accurate against `auth_routes.py`. Added 5 missing endpoints (`archetype-evolution-seen`, `alpha-feedback-prompt-seen`, `tutorial-alert-dismiss`, `tutorial-alerts-enroll`, `tutorial-alerts-increment`) to the constants list + endpoint table, plus the user-doc fields they touch (alpha-feedback gating, tutorial-alert lock/counters) and the expanded `/me` payload. Replaced the trailing raw "User Access Points" feature brief (typos, "I'll brief you later" instructions) with documentation of the implemented state — verified against `authBarInit.js` account modal and `account.html` (in-game display toggle still a disabled placeholder; Season History still a dead link).
- [x] `00_General_Systems/FTE_System.md` → **Verified, no changes 2026-06-12** — recent (May 2026) source-of-truth doc, fully accurate: `TutorialStep` enum, `tutorial-advance`/`tutorial-complete` endpoints, shot thresholds (10/110), all 9 frontend modules, debut publish helpers + `/api/community/debut`, feedback algorithm w/ Unconventional fallback, and cross-doc references all match code. The four superseded `projects/fte_*` files are confirmed deleted.
- [x] `00_General_Systems/original_thresholds.md` → **Deleted 2026-06-12** — restore-defaults reference card for the **jamies-cc (God mode) page, which no longer exists**: `config_overrides.py` is gone, no jamies-cc page in the codebase, and `constants/__init__.py` notes values are "now canonical here." Its values had also drifted from code (e.g. tempo slow mean 24 vs 20, max 35 vs 30). Current-value reference already exists: `05_GP_Supporting_Systems/Constants_System.md`; originals are recoverable from git history. No inbound references.

## 01_Game_Mode_Systems (5)

- [x] `01_Game_Mode_Systems/FCC.md` → **Updated 2026-06-12** — large FCC reference held up well (tab set, hero CTA state machine, Coach's Office cards, data wiring, endpoint inventory, legacy-hooks section all verified against `franchise-command-center.html/.js`). Synced the sections stale after the News-system build: Coach's Office News card (was "placeholder / In Development" → live 5-headline card with right-justified See All News link), Press tab section rewritten as the live News tab (`GET /franchise/news`, grouped by week, always visible; tab id still `press-tab`), `news_headlines` added to the command-center payload list. Also added the Recruits-tab YR column and corrected the recruiting-footnote wiring (now two live surfaces: `-home` / `-tab` id pairs; old hero ids gone).
- [x] `01_Game_Mode_Systems/Franchise_Mode_Systems.md` → **Rewritten + renamed to `Franchise_Mode_Overview.md` 2026-06-12** — old doc was a Jan-2025 layered fossil: 7-tab FCC description (real FCC has 14; duplicated `FCC.md`), an 80-line EOS section describing a dead 8-team week-15–17 bracket (live system: 26-week season + weeks 27–34 conference/region/national), self-contradictory storage claims (embedded `franchise_teams` vs FTD), and stale §3.5 (said training attrs load from embedded `franchise.players`; code reads the `franchise_players_data` collection). Rewritten as a slim top-level orientation map: 36-week lifecycle table, franchise state stores (franchise doc / FTD / FPD), game-init consumption path, pointers to detail docs, regression tests kept. Cross-refs to dead §3.5 in `Training_System.md` + `Data_Persistence_System.md` repointed with the corrected FPD flow.
- [x] `01_Game_Mode_Systems/Single_Game_Systems.md` → **Deleted 2026-06-12** (sunset mode). Skimmed before deletion: purely mode-specific (single-game team-object lifecycle in `games.{id}.teams`), nothing franchise depends on. Covered by new `Sunset_Modes.md`.
- [x] `01_Game_Mode_Systems/TCC.md` → **Deleted 2026-06-12** (sunset mode). Shared assets it described (`bracket.js` `renderBracketShared`, `scoutingReport.js`, `scouting_utils.py`) are noted in `Sunset_Modes.md` since franchise still uses them.
- [x] `01_Game_Mode_Systems/Tournament_Mode_Systems.md` → **Deleted 2026-06-12** (sunset mode). Purely mode-specific (tournament doc storage, TCC tabs, 8-team bracket). Covered by new `Sunset_Modes.md`.
- [x] `01_Game_Mode_Systems/Sunset_Modes.md` → **Created 2026-06-12** — sunset note: rebuild-from-scratch decision, deleted-doc list (git-recoverable), code-purge-deferred status, shared assets that live on in franchise, pointers to inline sunset tags in other docs.

## 03_Data_Persistence (7)

- [x] `03_Data_Persistence/Cache_Usage_Documentation.md` → **Verified + small updates 2026-06-12** — Jan-2026 cache inventory holds up well; spot-checked `ongoing_games`, `gameStore` settings cache + invalidation API, `stateTelemetry.js`, `_skeleton_cache`, FCC invalidation hooks (`invalidateFccTeamScopedCaches` / `invalidateHomeWeekSensitiveCaches`) and `fcc_session_cache_` — all match code. Added the new `fccNewsListCache` row (News tab) and tagged the Tournament cache/localStorage entries as sunset-mode wiring pending the code purge.
- [x] `03_Data_Persistence/Data_Persistence_System.md` → **Updated 2026-06-12** — heavily-maintained doc, largely current: FTD/FPD model, two-stage settings persistence (master store vs game-doc snapshot, April 2026 model), hybrid cache/DB approach, and the fix log through May 2026 all line up with code. Fixed fossil numbers in the franchise section (`week` 1-14 → 1-36 calendar, 14-week schedule → 26 weeks / 128 teams, "8 teams" → 128 in two spots). Tagged Tournament Mode and Single Game Mode sections as sunset (retained while code remains, per `Sunset_Modes.md`). Repointed reference block to `Franchise_Mode_Overview.md` and flagged the two legacy `docs/` tree references as predating the 128-team model.
- [x] `03_Data_Persistence/Player_Stats_Architecture_Update.md` → **Moved to `projects/` + status header 2026-06-12** — not documentation: an architecture proposal for a canonical franchise player view shape + shared backend resolver. Verified unimplemented: no resolver in `BackEnd`, and the mixed `season` / `season_stats` / `stats.season` reads it flags still exist across ~10 frontend files. Kept as valid future work (read-model consistency cleanup).
- [x] `03_Data_Persistence/Settings_Persistence_Guide.md` → **Keep (verified current) 2026-06-12** — April-2026 quick-reference for settings persistence; fully consistent with the two-stage model verified in `Data_Persistence_System.md` (master store vs game-doc snapshot, `user_team_object_id` vs canonical keys, GameManager-first fallback). Deliberate overlap: this is the readable entry point, the system doc is the deep reference + fix history; both point at each other. Referenced by `Game_Init_System.md` and `tasks/Defense_ID_Migration.md`. Added a sunset-mode note for the Tournament/Single Game mentions.
- [x] `03_Data_Persistence/State_&_Persistence_Contract.md` → **Updated 2026-06-12** — Jan-2026 state-management constitution (tiers, precedence, fail-loudly rules, inventory table, cadence plan); principles all still govern the implemented system. Fixed: status re-labeled from "Design Document / ready for review" to adopted contract (implementation done per work plan); FTD/FPD-era drift in the inventory table corrected (playbook/strategy settings → two-stage master-store sources, anchor attributes + franchise player stats → FPD, not embedded franchise-doc players); markdown bug fixed (a note was splitting the inventory table in half — moved below the table); sunset-mode note added.
- [x] `03_Data_Persistence/Unified_State_Persistence_Work_Plan.md` → **Archived to `projects/Z-Completed/` 2026-06-12** — completed work-tracking doc (phases 1.1–5.7 all done; phases 6–7 never executed and noted as such, with phase 6's tournament-training removal subsumed by the sunset code purge). Added archive note flagging that its top "Post-5.7 (Feb 2026) single source of truth" entry was superseded by the April-2026 two-stage settings model. All 6 cross-references in `Data_Persistence_System.md` + `State_&_Persistence_Contract.md` repointed; archive row added to Z-Completed README.
- [x] `03_Data_Persistence/timeout_data_&_state_persistence.md` → **Keep (verified current) 2026-06-12** — focused 4-step lifecycle walkthrough of timeout/quarter-break state (save → popup → lineup screen → resume restore). Spot-checked: FT-timeout persistence fields (`timeout_free_throws_remaining`, `timeout_one_and_one`, etc.), `apply_timeout_resume_state_to_gm`, and the foul-out edge-case doc reference all exist in code. Complementary, not redundant: `Data_Persistence_System.md` covers architecture + fix history, this covers the step-by-step data lifecycle. Added a missing related-docs block (hybrid model, Timeout_System, Computer_Timeout_System).

## 05_Animation_System (8)

- [x] `05_Animation_System/AG_Implementation.md` → **Delete (superseded + drifted) 2026-06-12** — implementation brief for AG → movement speed; work shipped and archived in `projects/Z-Completed/Movement_Rate_Refactor.md`. Core numbers no longer matched code after the curve retune: old curve `10 + AG/100×12` cap 30 vs actual `STANDARD × (0.90 + AG/100×0.2)` cap 60 anchored at 14; multiplier model (drive 0.75 / shot_motion 0.625) replaced by absolute archetype rates (13/14/14/18/32); cruise-16 + BH random(8,16) gone; §10 cited four retired constants. Current canon: `UESS_System.md` §3.4 / §9.3 / §11. Ported the one unique nugget (curve consumes effective AG = anchor × NG) into UESS §9.3. Repointed 5 inbound refs (Animation_System_Updated, Movement_Rate_Refactor, Fast_Break_Refactor, Core_Animation_System ×2) + 2 code comments + `docs/agents.md` (also dropped agents.md links to the deleted Active_Page_Analysis / Coordinate_Orientation_Audit docs).
- [x] `05_Animation_System/Advance_Triggers.md` → **Combined into `00_General_Systems/Step_By_Step_System.md`, then deleted 2026-06-12** — initially updated in place (fixed drift: FB pass rates sharp 30→40 / sloppy "hardcoded 22"→named constant 30 / HCO canonical 36→24; sprint rate 14→18; stale "Not yet migrated" framing for FCP/OREB/BIP/SIP/FT). Then merged into Step_By_Step to kill the dual-maintenance channel (same emitters documented in two places; the pass-rate drift was the proof). Merge ports: trigger vocab + FB pass-rate constants (doc intro), HCO gating-player rules + `result_type` → `turn_stop` map, HCT per-step trigger table, FCP/DREB gating notes, CR step-0/step-2 per-player movement detail, RR step-0 per-role targets + lane-pass/interception/bat-OOB movement detail, Triangle branch ATs. Corrections found during merge (verified in code): CR step-0 "others" drift 1–6 at `standard` (both old docs wrong — one said cruise, the emitter docstring said sprint); Triangle setup + drive steps gate on **BH** (not "RR+BH both" / "slower of BH,RR"); **RR outlet-denied cutback + recovery-pass steps moved to the next HCO turn's Reset step** (both docs + module docstring described the old 3-step shape). Fixed 4 stale code comments + repointed 8 inbound refs (constants, hct/rr/cr emitters, rr resolver, Fast_Break_System.md, Animation_System_Updated.md, Z-Completed README).
- [x] `05_Animation_System/Animation_Detection_Reference.md` → **Merged into `Animation_Routing_Reference.md` (holistic pass, 2026-06-12)** — was Keep (rewritten) 2026-06-12: — detection catalog for the `animateGameTurns.js` turn loop; no other doc covers this layer and it's still live (FE not yet pure renderer). Old catalog (16 detections, "stable as of May 2026") had drifted badly: missing 6 detections (CHARGE, DREB, TIMEOUT w/ loop break, FINAL_HOLD, final-turn FOUL, plus schema `animation_steps[]` clauses on FOUL/DEAD BALL/HCO/STEAL gates), claimed DEAD BALL never routes (it does when schema steps present), still documented the FCP/HCT direct-to-`playTurnAnimation` block (commented out in code — FCP/HCT now routes through the router like HCO), listed a removed duplicate FAST_BREAK legacy detection, and missed the removed SIP FastBreak-state skip. Rewrote the catalog in live execution order (18 detections), dropped line numbers in favor of durable predicates, verified handler names against `AnimationEngine`'s handler map (incl. `handlePutback` → `handleOrebTurn` delegation, `handleTimeout`, `handleFinalHold`).
- [x] `05_Animation_System/Animation_Handler_Reference.md` → **Merged into `Animation_Routing_Reference.md` (holistic pass, 2026-06-12)** — was Keep (updated) 2026-06-12: — companion to the detection reference; catalogs `AnimationEngine` handlers. Biggest gap fixed: the doc had **no mention of the schema playback dispatch** — `processTurn` checks `animation_steps[]` BEFORE `determineHandler` and schema turns bypass legacy handlers entirely (`runSchemaPlaybackTurn` → `playTurn` + `dispatchTurnStop`; FT turns get `_finishSchemaFreeThrowTurn`; un-migrated FB variants fall back). Added new section for that + hybrid-layer status note. Other fixes: registration list was missing `CHARGE`, `"DEAD BALL"` (space spelling), `FINAL_HOLD`, `FINAL_TURN_SHOT` + their handlers (`handleFinalHold`, `handleFinalTurnShot` documented); `determineHandler` order updated (FB → Final Turn checks → map → shot/rebound/pass → default, with predicate detail); `handleSideInbound` FastBreak-skip claim removed (skip was deleted; handler also stamps `_previousTurnWasInbound` / `_previousInboundTurnType`); `handleFreeThrow` schema guard + `quarter_ends_after` hold documented; dropped all drifted line-number references. Also corrected the detection doc's DREB row (the `_maybeRunDiscreteDrebOutletLeadIn` post-playback hook was removed from the schema path — caused double outlet execution).
- [x] `05_Animation_System/Core_Animation_System.md` → **Keep (updated) 2026-06-12** — FE animation architecture overview. Verified all referenced components/symbols exist (`AnimationRouter/Engine`, `BallController`, the 4 specialized systems, `passDetection`, `turnPreparation`, `courtClamp.clampGridCoords/isClampExempt`, `sanitize_turn_animation_payload`, `arrivalHeartbeat.ensureConsistentHeartbeat`). Same gap as the two reference docs: no mention of the UESS schema playback path. Added: two-path architecture diagram + explainer (schema turns short-circuit at `processTurn` → `playTurn`, handlers are the legacy path), schema row atop the tween-duration authority table (per-step `T_game_seconds`), legacy-path caveat on the Complete Routing Flow list, `animationPlayback.js` in Key Files. Tween-authority resolution order, clamp policy (x 9–91 / y 2–49, SIP/BIP/TIMEOUT exempt), heartbeat contract (75→750 BPM by NG), and coordinate flip rule all verified current.
- [x] `05_Animation_System/Player_Sprite_System.md` → **Keep (verified current, minor fixes) 2026-06-12** — recent (May 2026) and accurate doc for the headshot marker system. Spot-verified: both flags live (`USE_HEADSHOT_MARKER` / `USE_MARKER_V2_FEATURES` both `true`), `BALL_ATTACH_OFFSET = {0,0}` + `playerBallPos`, stamina ring tiers (>0.89 / ≥0.80 / ≥0.70 with nonlinear bands), height→radius formula (`30 + (h−72)×0.75`, clamp 25.5–39, default 28.5), `buildHeadshotInitialsInfo` in announcements.js. Fixed one internal contradiction: early initials-tile table said away tile = team secondary; the later shared-rule table + code (`createHeadshotMarkerV2.js`) say **white** — corrected. Also pathed the two bare `Game_Init_System.md` references to `../05_GP_Supporting_Systems/`.
- [x] `05_Animation_System/Step_Types_System.md` → **Merged into `00_General_Systems/Step_By_Step_System.md` § Universal step patterns (holistic pass, 2026-06-12)**; `reset_step_helper.py` spec pointer updated — was Keep (updated) 2026-06-12: — short vocabulary doc for universal step patterns (Parallel Movement, Pass, Reset, Shot Motion, Intercept, Batted Ball, Stopper). Live spec: `reset_step_helper.py` cites it as the Reset spec. Key finding: `build_reset_steps()` is fully implemented but has **zero call sites** — the live HCO seam still runs through the entry orchestrator's Handoff/Kickout/Walk Up (`transition_bridge.py`) + `hco_setup` hints; already tracked in `UESS_Backlog.md` item 14. Restructured the raw Reset notes into a proper spec section with an implementation-status banner, fixed cruise→`standard` rate (matches code), added RR Outlet Denied to the instances list, fixed typos. Also fixed `reset_step_helper.py` docstring ("cruise"→"standard", matching its own code). Holistic-pass candidate: could merge into `Step_By_Step_System.md` as an intro section.
- [x] `05_Animation_System/Transition_Systems.md` → **Keep (updated) 2026-06-12** — hold/delay reference for `animation_config.js`. Verified every config key, value, and usage site against code: 10 of 11 rows accurate (`shot.rimHoldMs/makeAnnouncementHoldMs/madeRimHoldMs`, `fastBreak.rimHoldMs/makeAnnouncementHoldMs`, `rebound.attachDelayMs` 500, `offensiveRebound.pauseMs` 1000, `inbound.holdAfterPlaceMs` 200 SIP-only, `freeThrow.rimHoldMs` 300 / `makeRimHoldMs` 1000, BIP fallback pass 250 ms). One drift: `fastBreak.defensiveStopHoldMs` doc said 1000, config default is **500** — corrected (and noted call sites' `?? 1000` fallback). Added terminal-quarter-end zero-hold detail, fixed stale footer path (`docs/docs_1_systems/...` → `../05_GP_Supporting_Systems/Announcement_System.md`), added hybrid-layer status note. Also fixed stale comment in `animation_config.js` ("SIP/BIP; applied twice" → SIP-only, single).

**Folder holistic pass (2026-06-12):** 8 docs → 4 final. `AG_Implementation` deleted (superseded), `Advance_Triggers` merged into `Step_By_Step_System`, `Animation_Detection_Reference` + `Animation_Handler_Reference` merged into new **`Animation_Routing_Reference.md`** (one hybrid-layer catalog: Part 1 detection, Part 2 handlers — kills the dual-maintenance drift channel), `Step_Types_System` merged into `Step_By_Step_System` § Universal step patterns. Remaining standalone: `Core_Animation_System` (architecture entry point), `Animation_Routing_Reference` (hybrid-layer catalog), `Player_Sprite_System` (visuals), `Transition_Systems` (tuning knobs). All inbound refs repointed (`Core_Animation_System.md` ×3, `reset_step_helper.py` docstring).

## 05_Features (6)

- [x] `05_Features/Defense_Matchups_System.md` → **Keep (updated) 2026-06-12** — user man-defense matchups popup + engine integration. Core verified accurate: all 5 util functions in `man_defense_matchups.py`, both API endpoints (`save-man-defense-matchups`, `lineup-for-matchups`), both `game_state` keys persisted via `summarize_game_state`, popup gating (`Q1 start / quarter break / timeout-foulout resume` AND Play Quarter only), drag-and-drop data+re-render pattern, stat strips (user DEF% / computer PTS rows). Fixed drift: position colors fully changed (was pink/orange/yellow/lightblue/purple → now PG blue `#4065AF` / SG purple `#7B5EA7` / SF green `#3A8C4A` / PF gold `#D4A017` / C red `#C0392B` + grey fallback); team headers (primary @0.2 bg / primary @0.6 border / white text, not secondary text); reset call sites renamed (unified timeout method in `game_manager.py` covers USER/COMPUTER/FOUL_OUT + quarter break in `simulate_quarter_endpoint`); added `shot_manager.py` to engine call sites. Added missing behaviors: don't-show-again persists via sessionStorage + resets only for truly-new game_id; FTE tutorial mode (`?mode=tutorial`) skips popup; once-per-game modal-open announcement SFX; music deferred until modal flow completes.
- [x] `05_Features/Lineup_Selection_Screen.md` → **Keep (verified current, minor fixes) 2026-06-12** — lineup screen + canonical autoset algorithm. Verified against code: fouled-out derivation (F ≥ 5 from game stats, no persisted list), NG thresholds (0.8 default / 0.64 late-Q4-under-4min + OT), foul limits `{1:1, 2:2, 3:3, 4:3}` with late-Q4/OT exemption, waterfall (NG −0.2 steps to 0, then foul limits +1/step cap 4), chemistry pool table in `_team_chemistry_pool_sizes` (all 4 bands exact), slot rating (position_ratings → trait-average fallback), NG color bands (>0.89 green / ≥0.8 yellow / ≥0.7 orange / red), all key files + functions exist (`/api/autoset-lineup`, `generateBothLineupsFromApi`, `_ensure_complete_lineup`, `fill_unified_lineup_gaps`, tests). Fixes: "5 Steps" header listed 4 steps → corrected; chemistry table bottom band relabeled ≤10 (code treats <7 same as 7–10) + TC cap-25/default-12 note; tagged Single Game / Tournament rows "(sunset)".
- [x] `05_Features/Playcall_Center.md` → **Keep (verified current) 2026-06-12** — in-game tactical hub. Verified against code: all DOM IDs/classes in `court.html` (44 matches incl. `DEFENSE_SCHEMES` order + "Man Normal"→"Man" mapping), `updatePlaycallCenter`/`clearPlaycallHighlights` in `playcallCenter.js`, `PlaycallOverrideRequest` + field-presence-only processing in api.py, override lifecycles (offense + tempo cleared after one use w/ `offense_override_cleared` flag; defense persistent + `_coerce_hco_defense_id` coercion; press/trap in `determine_defensive_pressure_type` press→FCP/trap→HCT/none→HCO user-team-only), `strategy_calls` structure incl. legacy keys, quarter-transition full reset (same api.py block as matchups reset). Only addition: Q4/OT situational Quick Shot override forces HCO with precedence over user press/trap override. Date header refreshed.
- [x] `05_Features/Player_Momentum_System.md` → **Delete (empty placeholder) 2026-06-12** — file contained only blank lines (2 bytes); never written. Code reality: player MO is **not a gameplay system** today — always 0 at game init (`player.py`), mutated only by training (`training_execution_v2.py`), clamped −10..+10, displayed via the red/green pill (covered in `Player_Attribute_System.md`, queued in the 06 folder). Per user (2026-06-12): momentum is a **future feature, not yet designed/built**. Note: `shot_manager.py` contains live legacy team-momentum code (`team_attributes["momentum"]` 0–10 shifts between teams in-game and modifies the 3PT threshold) — likely an early-build vestige; flag for removal or redesign when the momentum feature is built `[CODE-CLEANUP]`. No inbound doc references existed; write the doc when the feature is designed.
- [x] `05_Features/SFX_System.md` → **Keep (verified current, minor fixes) 2026-06-12** — consolidated SFX source of truth (already supersedes 3 archived docs). Verified against code: shot-launch tiers (<101 / >210 in `animation_step_helpers.py`), pass PS>75/<25 + reception (IQ+CH)>130/<50 tiers, **every** variant distribution table (outside/inside × make/miss × 3 tiers in `shot_variants.py` — exact match), FT variant tables + delta tiers + second-chance rules (`select_ft_shot_variant`), gap tier boundaries (±75/±150), bank y-offset boundary semantics, rattle extras (MSSS/RIM start, OPT_1/2), pre-rolled bank-miss SFX choice, all 14+ court stinger files + 50/50 and 33/33/34 splits in `gameSfx.js`, delay knobs (`BOR_MAKE_SWISH_DELAY_MS` 150 / `BANK_MAKE_SWISH_DELAY_MS` 100), announce resolver functions. Fixes: converted shipped "Replace All SFX files" work order to current-state note (`-lowervol` swaps done); documented the raw-`Audio()` airhorn exception (bootGame.js + AnimationEngine.js) to the playGameSfx-only claim; added `hot_shot_trail` stamp (>210) note. **Boundary cleanup vs `Sound_Design_System.md`:** absorbed the simulation-driven rows from that doc (timeout airhorn, foul/dead-ball whistle, shot-clock whistle); Sound_Design now covers UI-interaction sounds only and points here for gameplay audio.
- [x] `05_Features/Sim_Quarter_System.md` → **Keep (verified current, minor fixes) 2026-06-12** — sim-quarter results popup + scroll. Verified against `bootGame.js`: `showSimQuarterResults`/`handleSimQuarter` exist, `matchedEventIndices` once-per-event guard intact at top of the `find()` callback (the "do not remove" revert fix — confirmed lines 1016/1079), delays exact (2000ms events / 200ms clock-only / 2000ms completion), quarter filtering with quarter−1 fallback, displayQuarter = quarter−1, all 7 displayable event types incl. FCP/HCT shots via flags, team-color resolution (unified → fallback → `#ff6200`), `/api/simulate-quarter` + `full_sim` param. Added missing shipped behaviors: "Shooting foul!" suffix on misses w/ defensive foul + FTs; player headshots in scroll entries (env-aware path + generic fallback); OT period labels (`OT1`…). Removed stale Future Enhancement (steals/fouls already display). Date header refreshed.

## 05_GP_Supporting_Systems (30)

- [x] `05_GP_Supporting_Systems/Announcement_System.md` → **Keep (verified current, 2 fixes) 2026-06-12** — large, well-maintained doc (two-tier overlay architecture, UESS backend-stamped announcements, payload-carries-SFX). Verified: `#announcement-overlay-secondary` + `showSecondaryAnnouncementOverlay` + 2200ms in court.html, all `announceGameEvent` cases incl. secondary-tier routing, all 4 idempotent flags in live use (`_contextAnnouncementsShown`, `_reboundHeadlineShown`, `_airballAnnounced`, `_blockAnnounced`), foul language weights in `foulAnnouncementLanguage.js` match the taxonomy table **exactly** (offensive + defensive, lane/non-lane), `_airball_announcement` stamps in both emitters, "Quick Foul!" wired, initials per-player team resolution chain. Fixed the one drift (same as routing refs earlier today): two spots still claimed `_maybeRunDiscreteDrebOutletLeadIn` → `runDefensiveReboundSetup` runs after DREB `playTurn` — that hook was removed (double outlet); updated Rebound announcements ¶ + AnimationEngine Key Files entry.
- [x] `05_GP_Supporting_Systems/BIP_System.md` → **Updated (significant drift fixed) 2026-06-12** — HCO BIP setup verified exact (SF chemistry-aware y, PG 9–15 x / ±3 y offsets, defense_execution lane tiers 5/4/2, all coordinate ranges). Fixed major drift in both pressure branches: **HCT** no longer sources skeleton step 0 via `get_skeleton_for_turn` — dynamic HCT bypasses MongoDB skeleton (offense = `HCT_SETUP_POSITIONS`, defense = `hct_initial_defender_coords()`: PG center court + `HCT_STANDARD_NORMAL` centroids); **FCP** now uses `_build_fcp_setup_positions()` randomized ranges (`FCP_OFFENSE_SETUP_RANGES`/`FCP_DEFENSE_SETUP_RANGES`, chemistry-aware SF y, collision offset) — old static press/trap coords (PG x=80/x=60 etc.) deleted. Added UESS note: BIP emits 2-step `animation_steps[]` via `build_bip_animation_steps` + position snapshot; legacy `handleBaselineInbound` path retained as described. Corrected flip-formula claim (backend `getAwayTeamCoords` = 100−x; frontend legacy helpers = 101−x — noted the off-by-one between conventions). Marked `opp` machinery legacy-only. Named `_get_fcp_hct_post_inbound_start_index()` for the clock-start trim. Stripped all drifted line numbers. Quarter-possession pattern + pass-completion wait (600ms) verified accurate.
- [x] `05_GP_Supporting_Systems/Block_System.md` → **Keep (updated tuning drift) 2026-06-12** — design doc, implemented; core mechanics all verified accurate against `shot_manager.py` / `shared.py` / `ShotAnimationSystem.js`: height→0–10 mapping (`height_to_block_score`), scaled height term `(h×10)+rand(−9,9)`, `defense_block_score` weights .4/.4/.2 × rand(1–6), `shooter_finish_score` weights .4/.3/.2/.1 vs 250, block spot 2–15 back / ±6 y (clamped, uses `roles["shot_spot"]` first), BLOCK-as-MISS frontend audit (10 FE files check BLOCK alongside MISS), `_blockAnnounced` ordering, block-spot rimCoords. Fixed tuning drift: attempt roll is now `rand(1,4) < aggression` (doc said `rand(0,10)`), thresholds now **±150** (doc said ±200), and added the **undocumented secondary fight roll** (`rand(0,15) < team fight`) + `has_contest` gating. Noted momentum clamps (0–10) + cross-ref to the legacy team-momentum flag. Added BLOCK-skips-shot-variant detail.
- [x] `05_GP_Supporting_Systems/Computer_Team_Game_Init_System.md` → **Keep (updated) 2026-06-12** — short, mostly-accurate doc. Verified: `_init_strategy_settings()` weighted distributions (5/15/60/15/5 core; aggression 10/20/40/20/10; rebounding skews 3–4; trap/press skew 0–1; inside/attack/outside uniform 1–4; tempo per-game via `init_tempo_random()`), `SEEDED_OFFENSE_PLAY_IDS` = exactly 3 motion + 3 core set + 3 SF set plays ✓, `POSITION_FILTER_PLAY_IDS` six keys by play_id ✓, team play copy shape (play_id/name/play_type/play_focus/target_shooter/effectiveness/momentum/cloaking) ✓, `self.plays` still name-keyed (matches Rename Safety note) ✓. Fixed stale "Current behavior" bullets: seeded offense maps get **even distribution** (not empty motion dropdowns; no `slot_assignments` key in v2 init), added missing defaults (fast_breaks 33/33/34, zone even split, man_normal 100, `even_distribution_all`, schema_version 2, existence-filtered play_ids).
- [x] `05_GP_Supporting_Systems/Computer_Timeout_System.md` → **Updated (resolved internal contradiction + drift) 2026-06-12** — verified all foul conditions (Q1: 3f=100%/2f=30%; Q2: 4f=100%/3f=90%; Q3 same as Q2 but only ≤240s; Q4: 4f=90% only if 60<t<240) and all 7 energy conditions (counts 3/4/5 × high/mid thresholds → 50/75/90/80/90/95%, 3<low=100%; Q1–2 use .80/.70/.60, Q3–4 use .75/.65/.55; uses `NG` multiplier, active lineup only) — all **exact**. Verified Q4 time gate (≥240 no eval), pending/immediate dual-creation, persistence via `summarize_game_state`/serialize helpers. **Fixed two real problems:** (1) doc was self-contradictory on mode-gating — "Integration Points" wrongly claimed checks are wrapped in `if is_full_simulation:` and "NOT during Play Quarter," while the bottom sections correctly said they DO run in Play Quarter. Code: the check runs in **all modes**; `is_full_simulation` only gates *which teams* get checked (both incl. user in sim; non-user only in PQ) plus the immediate-vs-deferred creation branch. Rewrote Integration Points + Timeout Timing to match. (2) timeout-limit drift: doc said "Q1–Q3 max 1/quarter"; code is Q1–Q2 max 1, **Q3 max = timeouts−1**, Q4 max = timeouts. Fixed in both Base Constants and Timeout Limits.
- [x] `05_GP_Supporting_Systems/Constants_System.md` → **Keep (1 value fix + path fix) 2026-06-12** — value-reference card; checked every entry against `BackEnd/constants/__init__.py`. All accurate: turn-result fouls/steals (95/5/−135/−35/250/150/30), charge ±(−240/220), block thresholds ±150 + Y-roll 1–4, `AGGRESSION_FOUL_MULTIPLIER` {0:.8,1:.9,2:1,3:1.1,4:1.2}, shooting thresholds (50/110/.16/.4/.2/40), `TEAM_ATTR_RANGES` (shot_threshold 10–210, rebound_modifier 0–0.4), `TEMPO_PARAMS` (slow 20/6/5/30, normal 15/6/5/30, fast 10/4/4/15). Fixed: `DEAD_BALL_TURNOVER` doc said 30 ("#temp change from 7") but code = **10** — corrected + dropped stale note. Repathed `Attribute_Clamp_System.md` link from dead `docs/docs_1_systems/...` to `../06_GMO_Supporting_Systems/`.
- [x] `05_GP_Supporting_Systems/End_Of_Game_System.md` → **Keep (POTG brief→doc + path fix) 2026-06-12** — large, very well-maintained doc; deep franchise phase-A/phase-B split, distant-sim overrides, and EOG attribute logic all current. Spot-verified all team-attribute EOG formulas against `franchise_routes.py` `calculate_attr_changes`: shot_threshold (>50 −10/−5; >45 win −5/0 lose 0/5; else 5/10), discipline ((F+TO) vs opp+8 → 1/2, −3/−2, −1/0), fight (win 0/2, lose −3/−1), rebound_modifier (±8 TREB bands), team_chemistry (score_delta <4/<10/else) — **all exact**. Converted the bottom "Player Of The Game" **brief** (typo-ridden work order) into clean documentation — POTG is implemented in `FrontEnd/static/js/shared/potg.js` and matches the spec exactly (2pts/stat, DEF tiers at >10 att, 16-pt gap rule, 67/33 winner weighting, equal-weight same-team), plus documented the **seeded-per-gameId deterministic RNG** the brief omitted. Fixed stale `docs/docs_1_systems/00_Data_Systems/Games_Collection.md` → `../00_Data_Systems/`. (Other `docs/To Do/` refs verified still valid.)
- [x] `05_GP_Supporting_Systems/Energy_System.md` → **Updated (fixed real ND/IQ scaling error) 2026-06-12** — verified against `player.py`/`energy_system.py`/`api.py`/`game_manager.py`: all 10 `get_fatigue_decay_amount()` ND tiers **exact**, `decay/recharge/reset_energy` + `_rescale_attributes` (`int(anchor_k × ng)`) exact, recharge amounts all exact (quarter [0.07–0.12], halftime [0.15–0.20], timeout [0.03–0.06]), bench recharge 20/70/10 → 0/+.01/+.02, `omit_zeros` FCP/HCT defense logic, `recharge_all_players` (lineup+bench). **Fixed important drift:** doc claimed energy scales 12 attrs incl. **ND and IQ** in 3 places — actual `MALLEABLE_ATTRS` is **10** and excludes ND + IQ (plus NG/EM/MO/CH). Corrected all 3 + added the rationale (ND unscaled to avoid fatigue death-spiral). Flagged stale line-number citations (values verified current; api.py recharge is ~4691 not 2198, etc.).
- [x] `05_GP_Supporting_Systems/FB_Triangle_Play_Spec.md` → **MERGED into `Fast_Break_System.md` + DELETED 2026-06-12** — was a forward-looking design spec; Triangle is now fully implemented and the Bible (`Fast_Break_System.md`) already carried a complete Triangle behavioral subsection. Verified the spec's logic against `rim_runner_fast_break.py` + `triangle_step_emitter.py` (fb_open ×0.8, decision randint(1,8) branch table incl. drive_decision, corner-3 `190−fb_efficiency` override, corner shot-defender Euclid ≤6.0 — all exact). Per user decision (port-then-delete), ported the unique **phase-graph architecture framing** (entry→rr_read→setup→decision→finish ownership + "per-play phase maps inside one universal FB system" + HCO_STRING_SPOTS label set) into the Bible's Triangle subsection, then deleted the standalone spec. No remaining inbound refs except the Bible's provenance note.
- [x] `05_GP_Supporting_Systems/FCP_HCT_System.md` → **Keep standalone (1 fix) 2026-06-12** — true documentation doc (not a brief); distinct system from Fast_Break (defensive press-break resolution vs transition offense) and Step_By_Step (owns the resolution math they don't carry); clean ownership boundary with BIP_System (which references this doc for FCP setup ranges). Verified resolution math against `resolve_full_court_press_logic`/`resolve_half_court_trap_logic`: offenseScore PG 3×(BH×.6+AG×.2+IQ×.2) SG/SF 1×, defenseScore PG 3×(OD×.4+AG×.4+IQ×.2) SG/SF 1×, ×randint(1,6); BSM FCP **400** / HCT **200** +10×fight + pt_opp_modifier/pt_efficiency chem terms; DST FCP **600** / HCT **800** + discipline chem term; success/dominant gates + all outcome weight tables (D_FOUL/HCO/SHOT .3/.4/.3; O_FOUL/DBT/STEAL .2/.5/.3; turnover .6/.3/.1) — **all exact**. `FCP_OFFENSE/DEFENSE_SETUP_RANGES` + `FCP_SETUP_COLLISION_OFFSET_GRID=2` tables **exact**. **Fixed:** Base Constants line said dominant threshold `> 1000` — actual is DST 600/800+adjustment (detailed step 5 was already correct; summary was stale).
- [x] `05_GP_Supporting_Systems/Fast_Break_System.md` → **Keep (canonical "Bible"; 1 stale-enhancement fix) 2026-06-12** — huge, actively-maintained single-source-of-truth doc covering all 4 FB plays (covert_release / rim_runner / triangle / after_steal), defensive-stop vs shot logic, shot-contest grid, universal shot-geometry helper, and full stat tracking. Verified load-bearing constants exact: `SLIDER_TO_FAST_BREAK_PROB {0:0,1:.25,2:.5,3:.75,4:1.0}`, `DEFENSIVE_STOP_Y_RANGE=6` / `_DREB_OUTLET=8`, FB shot-spot bands (`FB_SHOT_SPOT_X_MIN/MAX=1/4`, `Y_RANGE=3`), shot-defender offsets (`X_OFFSET=2`, `Y_RANGE=2`). **Fixed contradiction:** Future Enhancements listed "Implement Triangle engine path" as pending, but Triangle is fully implemented (line ~178 + verified engine) — removed the stale bullet, clarified the user-FB-weights enhancement is the still-pending part. **Flagged for user:** this doc already contains a complete Triangle subsection → `FB_Triangle_Play_Spec.md` is now largely redundant (the spec's own closing line says fold it in here). Recommend retiring the standalone spec; awaiting user decision. **Follow-up 2026-06-13 (After-Steal recheck, surfaced during Steal_System review):** fixed real drift in the § "After Steal" section + steal references — the doc described the **legacy** steal model (5-10x steal-entry movement + ±6 defensive-stop band + break/stop skill check) and a **nonexistent `DEFENSIVE_STOP` branch** (step-back to top-of-key / "Great Stop!"). Verified vs `after_steal_fast_break.py` + `after_steal_fast_break_step_emitter.py`: the resolver returns **MAKE/MISS only** (no defensive stop), drives the stealer to a rim band (basket±2-3, y19-31), sends all 5 defenders to one spot with AG-traversal first-arriver freeze, then a contested/uncontested check; emitter builds a **single drive step** + post-shot sub-steps. Rewrote the "What" bullet to the real model, removed the false defensive-stop step-schema + step-back invariant (replaced with a "no defensive-stop branch" note), consolidated the shot-branch table to the single drive step, and tagged all legacy "±6 steal" / "steal entry" references (Base Constants, 8-step flow, Long Form) as superseded → point to § After Steal.
- [x] `05_GP_Supporting_Systems/Free_Throw_System.md` → **Keep (updated second-chance drift) 2026-06-12** — verified against `resolve_free_throw_logic` + `ft_step_emitter.py`: FT formula `(FT×0.8)+(CH×0.2)`, primary `roll < score → MAKE`, 1-and-1 unlock/end logic, bonus 5–9 / double 10+, `FREE_THROW_SHOT_GRID_PER_GAME_SECOND=12`, non-final-miss bounce `±5` offset (away rim 9→14, home rim 91→86), UESS schema step tables all accurate. **Fixed drift:** doc presented the miss-to-make secondary check as a flat **40%** (`random.random() < 0.40`); code now calls `effective_ft_miss_to_make_second_chance()` — home shooter = 0.40, **away shooter scaled by home crowd factor** (≤1→0.40, 2–3→0.30, ≥4→0.20). Updated all 3 spots (Base Constants, Step 3, long-form) + cross-ref to `Home_Crowd_System.md`.
- [x] `05_GP_Supporting_Systems/Game_Init_System.md` → **Keep (verified current, no changes) 2026-06-12** — exemplary, well-maintained doc (last updated May 2026). Spot-verified every concrete claim against code: `init_game()` + `load_ftd_data_for_team(franchise_id, team_id, team_name=None)` in api.py, `prepare_ftd_for_new_game(ftd)` in `franchise_ftd_game_seed.py`, Player height default `data.get("height", data.get("HT", 75))` exact, per-player `summarize_game_state` projection exact (playerId/name/team/team_id/pos/jersey/**height** [v2-sprite comment present]/photo/colors/x/y/stats/attributes **{EM,CH,MO,NG} only**), 3 players.append blocks confirmed. FTD→GameManager→game-doc flow, scoreboard enrich pipeline, and franchise/tournament/single seed paths all align. No content fixes needed. **Added SUNSET MODE tags** to the Tournament + Single Game init sections and the `mode` payload row, pointing at a new `bugs.md` tracked item for eventual removal of the sunset code paths (rebuild-from-scratch plan).
- [x] `05_GP_Supporting_Systems/Gameplay_Buttons_System.md` → **Keep (reframed dormant Sim Quarter + fixed stale samples) 2026-06-13** — verified handlers/classes (`handleButtonClick`/`handleSimQuarter`/`handleSimFullGame`; `.play-button`/`.sim-full-game-button`/`.sim-to-fourth-button`), quarter-break vs timeout-resume `resume_from_timeout` flow, and backend `turn_by_turn_mode = not body.full_sim` (api.py L3821) — all accurate. **Fixed significant drift:** doc framed Sim Quarter as a live "4th button," but `initGame()` (~L2736) unconditionally hides + disables `.sim-to-fourth-button` ("dormant for possible future reintroduction"); live UI is effectively 3 controls. Per user (approach 1) reframed Sim Quarter as DORMANT throughout (overview note, by-quarter table, section banner, Key Differences) rather than deleting (logic retained + shared `full_sim` path). Also corrected stale Critical Code samples: `simFullBtn` is always shown with label swap (no `>=4` hide), `sim4Btn` always hidden/disabled (not Q4 toggle); refreshed drifted line numbers (initGame ~2657, api.py 3821).
- [x] `05_GP_Supporting_Systems/HCO_Turn_Resolution_System.md` → **Keep (verified current, no changes) 2026-06-13** — accurate on both halves. Backend (`phase_resolution.py`): `play_id`-driven skeleton fetch, set-play alias remap `_build_set_play_alias_map(target_shooter)` → canonical `("PG","SG","SF","PF","C")` on `pos_actions` keys, variants `successful`/`mid_play_change`/`contested`/`broken` (successful via `steps` or `versions[]`), motion `base_loop` no-variants — all exact. Frontend (`gameScene.js`/`playcallCenter.js`): `gob:playcall-strip-show`/`-hide` dispatch + HCO gating, `renderPlaycallEvMeter`/`pcsEvPivotMarkup`, EV clamp [-100,100] + lit = `round(|EV|/10)` clamped 0–10, client never recomputes EV — all verified. No drift.
- [x] `05_GP_Supporting_Systems/Home_Crowd_System.md` → **Rewrote brief→doc (content verified accurate) 2026-06-13** — was a raw brief (typos, "I suggest we set…" voice, zero implementation detail) but fully built. Verified every number against `BackEnd/utils/home_crowd.py`: weight bands `_CROWD_WEIGHTS_BY_BAND` (7–10/11–15/16–20/21–25) + `_UPPER_BONUS_WEIGHTS` exact; impact table = `_shot_threshold_deltas_for_factor` (F3 away+25; F4 away+50; F5 away+50/home−50) + `effective_ft_miss_to_make_second_chance` (home=default; away F≤1 default, 2–3→0.3, ≥4→0.2) exact; CE shift `community_engagement_crowd_shift` (up/down/cancel, floor) + `consume_franchise_community_engagement_for_matchup` (clears pending flags) exact. Rewrote as clean documentation: roll/impact tables, `home_crowd_*` game_state keys + `HOME_CROWD_PERSIST_KEYS` resume-restore (no re-roll), and integration points (init in `game_manager.py`; CE consume + restore in `api.py`; shot delta in `shared.py`/`shot_manager.py`; FT in `phase_resolution.py`). Cross-refs `Free_Throw_System.md` + `Training_System.md`.
- [x] `05_GP_Supporting_Systems/Motion_Offense_Shot_System.md` → **Keep (line-number + label fixes; content verified) 2026-06-13** — all logic accurate vs `phase_resolution.py`: all 10 functions present (resolve_motion_offense_shot, _create_attack_drive_shoot_steps, _determine_attack_drive_destination, _check_inside/attack/outside_shot_possibility, _build_shot_type_weighted_list, _apply_attack_penalty, _create_pass_receive_step, _create_shoot_step), 300ms drive→shoot offset, `_attack_drive` dict + `stopped_short=False` TODO, drive-destination lists (upper/lower/central) exact. Attribute weights verified vs `constants/__init__.py` `PLAYCALL_ATTRIBUTE_WEIGHTS` (Inside SC6/ST2/IQ1/CH1, Attack SC5/AG2/ST1/IQ1/CH1, Outside SH8/IQ1/CH1) — exact. **Fixed:** all Key Files line numbers were badly drifted (e.g. resolve fn cited 3104-3298, actually ~4142) → updated to current ~start lines + noted approximate; pointed weights at constants; relabeled `animator.py` from "Frontend" to backend→frontend animation data (it's a BackEnd file).
- [x] `05_GP_Supporting_Systems/Possession_Mgmt_System.md` → **Keep (fixed real animation-fix drift + line numbers) 2026-06-13** — SS&S architecture verified vs `game_manager.py`: `possession_flips` flag, `switch_possession()` (~L1742), per-turn `offense_team_id`, double-flip-prevention guard `if next_play_type=="HCO" and possession_flips is True` (~L1199), all 5 flip locations present (SIP before setup_side_inbound ~L1338, DREB→HCO ~L1199, DREB→FB ~L1213, OREB ~L1014, Made→BIP ~L1454). Frontend verified (`turnPreparation.js` `handleTurnTransition` ~L229: pure assignment + `possessionChange` emit on change). **Fixed substantive drift:** doc said DREB→HCO and DREB→FB "update `offense_team_id` AFTER flip," but a later ANIMATION FIX in code does the opposite — the result turn deliberately **keeps `offense_team_id`=old team** for step-animation classification; new team shows on next turn via `game.offense_team`. Corrected both flow bullets + marked Critical Fixes 2/3 as superseded. Also refreshed all badly-drifted line numbers (e.g. simulate_macro_turn 249-600→~949, switch_possession 750→~1742, frontend 144-179→~229).
- [x] `05_GP_Supporting_Systems/Shot_Clock_System.md` (renamed from `Real_Time_Clock_System.md`) → **Trimmed + restructured + renamed (UESS overlap removed) 2026-06-13** — was a hybrid: ~half stale (legacy "clock categories" + Open Floor/COF/Drive/Compressed pace-constant table + Implementation-Plan Phases 1–4 + 700ms cadence) which **contradicts** UESS (ledger-derived `time_elapsed` §5.4; archetype/AG rates §3.4/§9.3; `tickMs=350` §9.4) and was explicitly retired by the May 2026 Movement Rate Refactor; ~half still live (shot-clock policy UESS doesn't cover). Verified live content vs code: `shot_clock_end`/`game_seconds_at_shot` derivation (turn_manager.py), `violation_threshold = 60+chem+disc+IQ/4` (phase_resolution ~L4605), Motion `recalibration_score = chem×5+disc×3` (~L4588, `_motion_shot_recalibrated`), timeout min-reconciliation, post-make BIP run-through. Rewrote as a focused **Shot Clock System** doc (H1 retitled; filename kept so inbound refs hold), deferring all elapsed-time/movement to UESS via scope note + cross-refs. **Open question for user:** standalone vs roll into UESS.
- [x] `05_GP_Supporting_Systems/Rebound_System.md` → **Keep (cleaned brief section + 1 real drift + relabel) 2026-06-13** — verified core vs `shared.py`/`shot_manager.py`: post-shot overlay maps + `TURN_COORDS_OVERLAY_KEYS`, `FREE_THROW_REBOUND_MAX_X_DELTA=20` + x-gate/fallback in `determine_rebounder`, `choose_rebounder` (closest-to-bounce, shooter ×1.2 penalty), `calculate_rebound_score = RB×0.5+ST×0.3+IQ×0.2 ×randint(1,6)`, selection `def_prob=min(0.95,max(0.55,0.75+bias))` + `d_weight` math — **all exact**. Bounce variance (short<15→x2-6/y±6, medium≤20→x2-8/y±8, long>20→x2-10/y±10) exact. Frontcourt-half x filter (HCT/FCP/FB, x≥50/x≤50) confirmed implemented (shot_manager ~L1585-1599). **Fixed real drift:** the "8 Steps" brief claimed a zone penalty `def_weight *= 0.9` that does **not** exist in code (and step 8's own formula omitted it) — removed it; also fixed the typo-ridden brief (Calculdate/mised/radom) into clean prose with code refs and dropped the stale `**(need to account for distance here)**` TODO. Relabeled "Rebounder selection per turn type (target state)" → **(current state)** since the filters are now implemented. (Spot-checked, not exhaustively re-derived: OREB putback shot-defender IQ-read + OTB foul thresholds — left as-is.)
- [x] `05_GP_Supporting_Systems/SIP_System.md` → **Keep (added UESS-migration reframe; data verified) 2026-06-13** — backend position data **exactly accurate** vs `setup_side_inbound()` (~L555): inbound_spot {47,48}, offense ranges PG x(50,54)/y(37,43), SG x(55,64)/y(18,32), PF x(65,80)/y(26,36), C x(65,80)/y(14,24); defense fixed PG{60,25}/SG{64,33}/SF{66,17}/PF{80,25}/C{85,28}; `getAwayTeamCoords` flip — all match. **Gap fixed:** doc framed the legacy `handleSideInbound`/`runSideInboundSetup` renderer as THE path, but SIP is UESS-migrated — `setup_side_inbound` also emits `animation_steps` via `build_sip_animation_steps` (2-step: setup walk-in + inbound pass, gated on all 10, no clock burn) and schema playback is primary; legacy path is now fallback. Added top migration banner, reframed Process Overview + Key Functions/Files as legacy-fallback, cross-ref UESS §2/§4, fixed drifted backend line ref (63-137 → ~555) and softened frontend line refs.
- [x] `05_GP_Supporting_Systems/Shot_Clock_Audit_and_Work_Plan.md` → **DELETED 2026-06-13** — completed point-in-time audit + work plan, not reference doc. Its sole remediation (backend shot-clock authority / remove frontend duplicate reset logic) is **done**: `shouldResetShotClockOnTurn` and `syncToThirty` no longer exist; frontend uses single backend-value rule (`incomingShotSec` → `syncWithBackend`); only kept the pause behavior the plan said to keep (`isNoImpactShotClockTurn → pause`). Durable shot-clock rules now live in `Shot_Clock_System.md` (which this doc already cited as source of truth). Deleted to avoid a stale work plan drifting from the canonical doc.
- [x] `05_GP_Supporting_Systems/Shot_System.md` → **Keep (fixed 3 real drifts + internal contradiction) 2026-06-13** — big doc; verified core vs `shot_manager.py`/`constants`: zone deltas `_hco_zone_shot_threshold_delta` (2-3 inside+25/attack+10/outside−25; 3-2 inside−30/attack−30/outside+50) exact, rim box `RIM_BOX_HALF_SPAN=6` ✓, attribute weights (Inside SC6/ST2/IQ1/CH1 etc.) ✓, defense-score formulas (paint/three/mid) ✓, screen ×0.15, gravity ×0.02, variant mods ✓, foul thresholds (50/110,70/130,30/90) ✓. **Fixed:** (1) **contest box drift** — doc said `|Δx|≤6/|Δy|≤4`; actual `CONTEST_DEFENDER_DX_MAX/DY_MAX=8` (8×8), AND doc missed that for HCO/Final Turn `has_contest=bool(defender or second_defender)` (role-based), geometry box only used non-HCO — corrected top "Location" item 3 + Note 6. (2) **internal contradiction**: top "13 Steps" had three-point `40−` + defense penalty `0.6`/`0.35`; bottom "Long Form" had `100−` + `0.2`/`0.14`. Code: `THREE_POINT_SHOT_THRESHOLD_INCREASE=40` and main `calculate_shot_score` uses 0.6/0.35+0.35 (top correct) — fixed bottom Step 4/Note 3 (100→40) and Step 7e (0.2/0.14→0.6/0.35), noting the ×0.2 ID-only penalty is the separate `resolve_fast_break_shot` path.
- [x] `05_GP_Supporting_Systems/Sim_Playcalling_System.md` → **Keep (2 small fixes) 2026-06-13** — high-level architecture doc, accurate vs `turn_manager.py`: offensive/defensive override flow (`strategy_calls["offense_call"]`/`["defense_call"]`), motion-vs-set weighting by `strategy_settings["offense"]` 0–4 (100/0…0/100), focus selection, `current_playcall`/`defense_playcall`/`offense_play_type`/`offense_play_focus` game_state keys, zone selection via `_select_zone_defense_with_playbook_weights`, play_id-first w/ name fallback. All 4 referenced files exist. **Fixed:** (1) `playbook_settings` structure example omitted the `man_defense` + `zone_defense` sections (both exist + normalized in `gameplan_routes.py` L67; doc's own Defensive Flow references `zone_defense`) — added them + a note on the 5 weighted sections and canonical zone keys (`zone_23`/`zone_32`/`zone_131`). (2) tagged the Single Game / Tournament rows in Mode Storage as sunset w/ pointer to `Sunset_Modes.md`.
- [x] `05_GP_Supporting_Systems/Situational_Logic_System.md` → **Keep (1 real drift + code-comment path fix) 2026-06-13** — detailed, well-maintained Q4/OT doc (code cites it as source of truth). **Time-band table verified EXACT** vs `constants.SITUATIONAL_TIME_BANDS` (2:01–3:00 slow≥12/quick −24..−12; 1:01–2:00 slow≥9/quick −18..−9; 0:31–1:00 slow≥3/quick −12..−3/FF 3<Δ<12; 0:01–0:30 slow≥1/quick Δ<−3/FF 0<Δ<9) + `situational_logic.py` predicates (`is_slow_it_down`/`is_quick_shot`/`should_force_foul`, last-30 trailing-by->3 rule, down-exactly-3→Final Shot). Force-foul time 1–3s, BIP/SIP receiver SG ✓. Selection fns `_select_motion_play_situational_slow`/`_select_set_play_situational_quick_shot` in `set_playcalls()` ✓. Final Turn: shooter weights 50/30/20/9/1 (`resolve_final_turn_shot_logic` L4363) ✓, blocking-foul=2-FT-attack-only (`game_state["final_turn"]`) ✓, FINAL_HOLD/`quarter_ends_after` exist. **Fixed drift:** doc said Final Turn hold = **3 seconds** / `holdFinalShotMs` default **3000 ms** in 3 spots; `animation_config.js:88` sets it to **2000** (consumers `?? 3000`×2 / `?? 2000`×1 never fire since the value is set) → corrected to 2 s / 2000 ms with the fallback nuance noted. Also repointed the stale `docs/docs_1_systems/...` doc path in `situational_logic.py`'s module docstring to `_documentation_master/...`.
- [x] `05_GP_Supporting_Systems/Special_Tracking_System.md` → **Combined + deleted 2026-06-13** — content verified exact (immutable `opening_lineup` snapshot: `snapshot_opening_lineups_to_game_state()` in `BackEnd/opening_lineup_snapshot.py`, called from `opening_tip.py`, persisted via `summarize_game_state`, restored in `api.py`; shape `dict[str, list[str]]`; consumed by PGPC). Per user, folded into `03_Data_Persistence/Data_Persistence_System.md` as a new **"Special Gameplay-Tracking Fields"** subsection (extensible table for future special-tracking fields; filled a gap since that doc didn't mention `opening_lineup`). Standalone deleted. Repointed the 2 inbound refs (stale `docs/docs_1_systems/...` paths in `opening_lineup_snapshot.py` docstring + `docs/To Do/PCPG_Workplan.md`) to the new location.
- [x] `05_GP_Supporting_Systems/Steal_System.md` → **Trimmed + reframed (major drift from after-steal UESS migration) 2026-06-13** — per user (trim+repoint), rewrote into a focused "steal resolution + routing" doc. **Verified current:** steal sets `last_stealer`/`last_stealer_coords` (`_check_steal_attempt`, FCP/HCT/HCO); the single routing roll uses the **stealing team = `def_team`** aggression via `fast_break_probability_from_slider` / `SLIDER_TO_FAST_BREAK_PROB` (0/25/50/75/100) → FAST_BREAK or HCO (code comment "Stealing team = def_team", L2488). **Cut as superseded:** the entire legacy "Steal Entry" FB path (5–10x/±4y move + DREB-style ±6y stop + `animateStealEntry`) — `resolve_fast_break_logic` short-circuits all steals to `after_steal_fast_break.py` (UESS schema, totally different spec: BH drives to basket_x±2-3, all 5 D sprint to one spot, AG traversal/first-arriver, contested check); `animateStealEntry()` removed + `STEAL_ENTRY_*` no longer imported FE; legacy steal-entry block L1195-1360 is unreachable. FB path now points to `Fast_Break_System.md` §After Steal. **Steal→HCO setup:** backend still computes it (`resolve_half_court_offense_logic` ~L4820: stealer away 3-7x/±3y; **bespoke PG repositioning when BH≠PG** [doc had omitted this]; others 15-30x/±6y) and stamps `is_steal_hco_setup`+coords — but **FE has zero references** to `animateStealHCOSetup`/`is_steal_hco_setup`/`*hco_setup*` fields (dedicated animator removed). Documented as backend-computed-but-unrendered + flagged as cleanup candidate / verify-render. Constants split into live (STEAL_HCO_SETUP_*) vs legacy (STEAL_ENTRY_*). Key Files refreshed. No inbound refs to repoint.
- [x] `05_GP_Supporting_Systems/Stopper_System.md` → **Keep (verified + significant drift fixed) 2026-06-13** — Jan-2025 doc; backend truncation architecture verified accurate (all SS&S helpers + `apply_stopper_system_to_skeleton` exist; deep-copy, stop-step selection, ball-handler/defender determination, possession-flip-flag-only pattern all match `phase_resolution.py`). **Fixed real drift:** (1) FCP/HCT result-determination math was stale — `+500`/`+300` success gates → actual BSM (400 FCP / 200 HCT +10×fight+chem), `>1000` dominant gate → DST (600 FCP / 800 HCT +disc/chem), dominant weights `30/50/20` → `30/40/30` (same values I corrected in `FCP_HCT_System.md`); trimmed the duplicated math to a summary + pointer to kill the dual-maintenance channel. (2) Added missing `SHOT_CLOCK_VIOLATION` result (HCO per-step clock check L4610 → stopper, mapped to `dead_ball_turnover`, precomputed `shot_clock_violation_step_index`). (3) Turnover/steal stop-step is now a **blast radius (middle ±2 random)**, not the exact middle. (4) **UESS reframe (biggest gap):** doc framed legacy `playTurnAnimation()`/`skeleton_to_animations()` as THE renderer, but HCO/FCP/HCT stopper outcomes are now schema-rendered via `build_skeleton_animation_steps` (`_emit_hco_animation_steps` / `turn_type="FCP"`) + `build_dynamic_hct_animation_steps`; HCO Phase 2 drops legacy `animations[]` entirely; emitter has dedicated FCP stopper-step gating (`_is_fcp_stopper_action_step`). Added top render-status banner + reframed Frontend/Animation-Generation sections as legacy fallback. (5) Refreshed all badly-drifted line numbers (apply_stopper 2565→~3502, HCO 3366→~4479, FCP 4402→~5724, HCT 5482→~7155).
- [x] `05_GP_Supporting_Systems/Timeout_System.md` → **Keep (verified + fixes 2026-06-13)** — large Jan-2025 + Feb-2026 doc; architecture verified accurate (all core fns exist: `setup_timeout_turn` turn_manager ~L3417, `call_timeout` game_manager ~L239, `handle_timeout_save_and_response`/`restore_timeout_resume_state`/`apply_timeout_resume_state_to_gm` api.py ~L1131/1031/1357, `simulate_quarter` genuinely in `BackEnd/main.py` ~L336; unified handler, DB-as-source-of-truth resume, URL-param-as-frontend-truth, eligibility/foul/energy filtering, computer auto-rebuild all match code). **Fixed:** (1) real error — late-Q4(<240s)/OT energy threshold is **0.64**, not 0.69 (verified `is_player_eligible_for_lineup`, db_utils L70-71; matches `Lineup_Selection_Screen.md`); foul limits `{1:1,2:2,3:3,4:3}` confirmed. (2) `ensure_complete_lineup` renamed/relocated → `_ensure_complete_lineup` (`BackEnd/main.py`) + `fill_unified_lineup_gaps` (db_utils). (3) Wrong test reference — `test_timeout_functionality.py` gone → listed the 4 real timeout test files. (4) Refreshed drifted backend line numbers + generalized FE ones (added caveat that FE `(lines …)` weren't exhaustively re-verified). (5) Repointed stale `docs/docs_1_systems/...` path; `docs/To Do/player_foul_out_bug.md` confirmed still exists (kept). (6) Tagged Single/Tournament mode references SUNSET. Complementary to `Computer_Timeout_System.md` (AI trigger conditions) + `timeout_data_&_state_persistence.md` (step-by-step lifecycle) — this is the master mechanics doc, no redundancy.
- [x] `05_GP_Supporting_Systems/Turn_by_Turn_System.md` → **Keep (verified + drift fixed) 2026-06-13** — Feb-2025 doc, already UESS-aware (three-bucket model, discrete-`DREB` turn, coords-sync funnel). Verified all referenced fns exist: `_append_turn` (game_manager L542), `_build_dreb_turn_from_miss` (L590), `sync_lineup_coords_from_turn`/`apply_coords_from_animations_list` (shared L2957/L2926), `run_micro_turn` (turn_manager L1122), `build_dreb_animation_steps` (dreb_step_emitter L74); `simulateTurnByTurn` still in gameScene.js; `offensive_efficiency`/`defensive_efficiency` are real team attrs. **Fixed real drift:** doc described a now-**removed** post-DREB frontend outlet lead-in (3 places: Key Files, `dreb_outlet_pass` bullet, discrete-DREB step 4). Per `AnimationEngine.js` L338 note, `_maybeRunDiscreteDrebOutletLeadIn`/`runDefensiveReboundSetup` is no longer called after the schema DREB row (caused double-execution vs schema handoff); the BH→PG handoff/walk-up is now owned by the next HCO turn's schema entry orchestrator. (`runDefensiveReboundSetup` still used for non-discrete FT-DREB + FB-DREB shot-turn paths.) **Also:** repointed stale `docs/docs_1_systems/...HCO_Turn_Resolution_System.md` → same-folder; fixed `docs/UNIFIED_DATA_STRUCTURE_ANALYSIS.md` → `docs/Data_Docs/...` (verified both legacy-tree refs exist); softened drifted line numbers (Bucket 1 423-650 → `run_micro_turn` ~L1122, etc.). **05_GP_Supporting_Systems folder complete (30/30).**

## 06_Features (2)

- [x] `06_Features/Coaching_Grid.md` → **Keep (reframed as placeholder/orphaned + terminology fix) 2026-06-13** — `coaching-grid.html/.css/.js` exist and render, but it's a **placeholder prototype**: reads hard-coded `data-*` attrs (no API), and **nothing links to it** (orphaned — only its own files reference `coaching-grid`). Verified the JS: functions are `commandScoreToY()` / `momentumToX()` / `positionDots()`, data attr is **`data-command`** (not `data-effectiveness`/`effectivenessToY` as the doc claimed) — fixed terminology throughout (command score = the 0–100 vertical axis). It visualizes the **four coaching-*focus* archetypes** (Authoritarian/Systems/Player Maximizer/Culture, present in backend training code), a **separate system** from the per-user 18-archetype badge system (`Coaching_Archetype_System.md`) — added cross-refs warning not to conflate. Flagged the `coaching` object Data Source as aspirational (per-archetype `momentum` amplifier is a commented TODO in `training_execution_v2.py`). Repointed 2 stale `docs/docs_1_systems/...` paths; sunset-tagged Single/Tournament. **Candidate to wire-up or remove — user's call.**
- [x] `06_Features/Statistics_System.md` → **Combined + deleted 2026-06-12** — was a small play_id-migration supplement that pointed to the main doc; its unique content (play_id-first rollups, training-report delta keys, canonical-rebounder putback fix) folded into `00_General_Systems/Statistics_System.md`'s Play Identity Note.

## 06_GMO_Supporting_Systems (29)

- [x] `06_GMO_Supporting_Systems/Attribute_Clamp_System.md` → **Keep (verified, no changes) 2026-06-13** — concise clamp-range reference, **100% accurate** against code. Verified `PLAYER_ATTR_CLAMP=(1,None)`, `TRAINABLE_PLAYER_ATTRS = ALL_ATTRS−{EM,MO,NG}` (the 13 listed), `clamp_mo` MO −10..+10 (`player.py`), and all 12 `TEAM_ATTR_CLAMPS` entries exact (shot_threshold 10–210 + rebound_modifier 0–0.4 via `TEAM_ATTR_RANGES`; team_chemistry 7–25; the nine efficiency/modifier/fight/discipline at −10..10). No stale paths or line numbers. Clean.
- [x] `06_GMO_Supporting_Systems/Championship_Announce_Moments.md` → **Keep (verified + link fixes) 2026-06-13** — accurate, well-structured. Verified all 3 enqueue fns exist (`franchise_championship_moments.py` L408/437/495), both endpoints + `pending_championship_moments` (franchise_routes), frontend `window.ChampionshipMoments` API (`showMoment`/`processPendingMoments`/`dismissOnServer`), and the two integration call sites (`processPendingMoments` in FCC, `championship-moments/context` in gameCompletionPopup). **Fixed:** the visual-source link pointed to `../projects/Championship Announce.html` but the file is in `projects/Z-Completed/` — repointed; and **all 5 hyperlinked `#L` anchors had drifted** — updated to current (tournament_progression 449→458, franchise_routes 3873→4186 & 10897→12117, FCC 2772→3039, gameCompletionPopup 51→32).
- [x] `06_GMO_Supporting_Systems/Coaching_Focus_Implementation_Map.md` → **Keep (verified, 2 small fixes) 2026-06-13** — detailed "code truth" status doc for the 16 coaching-focus leaves; **highly accurate**. Verified all referenced fns exist (`parse_coaching_focus` L342, `_should_amplify_player_attr` L1459, `_should_amplify_team_attr` L1517, `apply_play_defense_training` L2047, `_apply_player_training_points` L842, `positional_focus_attrs_for_player` L223, `normalize_coaching_focus_custom_by_player` L229, `coaching_focus_leaf_display_name` L335); Community Engagement plumbing (`pending_community_engagement` + `consume_franchise_community_engagement_for_matchup` in home_crowd.py / franchise_routes); **positional-focus triples exact** (`POSITIONAL_FOCUS_ATTRS_BY_PRIMARY` PG→PS/BH/IQ … C→SC/ID/ST + PG→C tie-break). **Fixed:** `momentum_score` TODO line ~507–509 → **~L724**; repointed 2 stale `docs/docs_1_systems/...` paths (Training_System same-folder, Home_Crowd → `../05_GP_Supporting_Systems/`). **2026-06-13 (b):** user asked (at Training_System review) whether to roll this into Training_System → decided **keep separate** (it's the scannable code-truth status matrix; design prose stays in Training_System); reciprocal cross-link added in Training_System's Coaching Focus Amplifiers section.
- [x] `06_GMO_Supporting_Systems/Community_Highlights_System.md` → **Keep (verified, 1 fix) 2026-06-13** — spec/impl doc for the Mode-Select Community Highlights feed; **fully built & accurate**. Verified all backend fns (`community_highlights.py`: `build_community_highlight_pending` L194, `flush_community_highlight_pending_after_week` L589, `_build_standard_entry` L394, `_user_regular_season_record` L281, `lead_archetype_for_user` L99, `record_archetype_change_if_any` L111, `_build_archetype_entry` L536), franchise_routes fns (`_apply_regular_season_rank_prestige_updates` L205, `_complete_week_process_user_game_block` L3450, `_complete_week_finish_cpu_and_persist` L4591, `use_franchise_rank_prestige_v2`), and frontend `renderCommunityHighlights` (mode-select.js L439, called L541). No line numbers to drift (uses fn names), no stale `docs/` paths. **Fixed:** stale opening line ("currently 'Coming Soon' placeholder") — the feed is now implemented (body populated by `renderCommunityHighlights`, placeholder gone).
- [x] `06_GMO_Supporting_Systems/Court_Template_Implementation_Spec.md` → **Keep (verified + absorbed sibling) 2026-06-13** — forward-looking design spec for deterministic court-image generation; **feature NOT built** (no renderer/`hardwood_variant`/`generate_court` in code). Runtime anchors/clamp verified exact vs `courtClamp.js` (`CLAMP_BOUNDS` 5/95/2/49); 8 A1 reference court JPGs exist. Added not-built banner. **Now the single court-template spec:** the high-level framing from `Court_Template_Spec.md` (feasibility conclusion, Always/Sometimes decoration layers, independent color layers) was folded in as a new "Design Strategy" section.
- [x] `06_GMO_Supporting_Systems/Court_Template_Spec.md` → **DELETED (merged) 2026-06-13** — per user decision, combined into `Court_Template_Implementation_Spec.md` (the implementation spec already superseded ~70% of it; unique high-level bits folded in). No remaining references.
- [x] `06_GMO_Supporting_Systems/Distant_Game_Sim_Player_Stats.md` → **Keep (verified + implemented, minor fixes) 2026-06-13** — player-stat distribution spec, **now fully built** in `BackEnd/models/distant_game_stats.py` (every function present: identify_starters, distribute_minutes, distribute_stat, all team/player weight fns, shooting breakdown, reconcile_team_shooting, + orchestrator `build_distant_game_summary`). **Constants table 100% exact** vs `computer_game_constants.py` (69/25/58/44/5/16/33/13/19/68/31/9/22/18/4/2/19/9). **Fixed:** added verified+implemented banner; corrected stale "New file for all functions" framing; flagged 3 drifted signatures (`identify_starters`→`(set,dict)`, `calculate_team_blocks(constants,opponent_fga)`, `calculate_team_fouls(constants,opponent_fta,minutes)`); repointed stale `docs/docs_1_systems/...` Key Files path. Defensive Adjustment Layer still unbuilt (accurate).
- [x] `06_GMO_Supporting_Systems/Distant_Game_Sim_System.md` → **Keep (verified + implemented, banner + superseded-notes flag) 2026-06-13** — scores/winner/margin spec, **built & exact**. Verified `_run_distant_game_sim` (L2004: margin buckets <0.18/<0.45/<0.77/else, gap ×1.25/×1.50, score clamps 39/121), win-prob inputs in `_distant_sim_team_combined` (L1987: base = prestige + 0.1×total_player_attrs; momentum mult bands clamp 7–25 → 1/2/3/4/6 exact at L1961; home edge 2×team_chemistry L1930), next-opp override `_user_next_regular_season_opponent_id` (L3125), `simulation_engine="distant"` branch (L1104). **Fixed:** added verified+implemented banner; **deleted the obsolete bottom "working notes" block (500-template approach — never built; per user OK), replaced with a one-line pointer to the player-stats doc.**
- [x] `06_GMO_Supporting_Systems/Distant_Team_Training_System.md` → **Keep (verified, header + notes added) 2026-06-13** — concise conceptual doc, **fully accurate** vs `_apply_franchise_distant_cpu_training` (~L10176). Verified: `distant_training` collection source; `team_values`→`team_attributes` with `TEAM_ATTR_CLAMPS` (rebound_modifier float-rounded); `players.player_{i}` deltas→FPD `attributes.*`+`anchor_*` with `PLAYER_ATTR_CLAMP` floor; position-rating recompute; optional `pending_community_engagement` (template `community_engagement` flag); iterate-non-user + skip-eliminated-EOS; FTD-`players`→team `player_ids` fallback ordering; correctly does NOT touch plays/scouting/CPU training reports. **Fixed:** added verification header + small precision notes (random template per team, `training_type` tc/regular, idempotency via `cpu_distant_trained_week`, 12-player cap, template field shape).
- [x] `06_GMO_Supporting_Systems/EOS_Write_Path_Inventory.md` → **Keep (verified, dated) 2026-06-13** — living "who-mutates-EOS-state" code-path inventory; **100% accurate**, every function/endpoint/script confirmed present (`_eos_calendar_advance_update_fields` L4002, `_finalize_franchise_week_after_cpu_games` L4087, both heal helpers L4375/L4467, `_training_status_reset_after_advance_to_week` L121, `_sync_eos_bracket_from_existing_game_doc` L3837, endpoints sim-rest L11740 / sim-championship L11963; ftp `record_tournament_game_result` L258 / `advance_conference_bracket` L240 / `advance_national_bracket` L248; `get_eos_week_games` L432; repair fn L133 + scripts/ repair). **Fixed:** upgraded the "Last reviewed" line to a dated verification with current line numbers.
- [x] `06_GMO_Supporting_Systems/End_Of_Season_System.md` → **Keep (verified, header + cross-refs) 2026-06-13** — week 35/36 flow + season rollover. Verified All-American logic exact vs `_compute_all_american_teams`/`_season_awards_score` (~L8842/L8821): season stats, DEF% bonus gated on `DEF_A >= 130`, 1st=top5 / 2nd=6-10 / 3rd=random 5 of ranks 11-20, persisted once (`_persist_week_35_awards_if_needed`, no reroll), `awards` field; coaching-focus carryover `round(prior×0.25)` exact (`carryover_coaching_focus_counts_for_new_season`). **Fixed:** added title/verification header; cross-linked `Recruiting_System.md` (authoritative recruiting) + `Season_Init_System.md` (rollover) since the week-35 recruiting section summarizes those.
- [x] `06_GMO_Supporting_Systems/Franchise_Tournament_System.md` → **Keep (verified, header + 1 ref fix) 2026-06-13** — EOS tournament structure + sim-routing + bye-modal doc; **accurate**. Verified phase week boundaries exact (`EOS_CONFERENCE_WEEKS=(27,28,29)` / `EOS_REGION_WEEKS=(30,31)` / `EOS_NATIONAL_WEEKS=(32,33,34)` / `REGULAR_SEASON_WEEKS=26` in `franchise_tournament.py`), `get_eliminated_team_ids` (L1191), all bracket-status + bye-modal fields present in `franchise_routes.py`, `/franchise/sim-rest-of-tournament` endpoint, `run_franchise_training` (L10536). **Fixed:** added verification header; refined the elimination-skip reference (skip lives in `_apply_franchise_distant_cpu_training`, reached via `run_franchise_training`, not the route's own loop). **2026-06-13 (b):** user asked if this + `Tournament_Execution_System.md` should merge → decided **keep both standalone** (different altitudes: rules/design here vs engineering/execution there, only week-map overlaps); added reciprocal scope/lane cross-link here (→ Tournament_Execution + EOS_Write_Path_Inventory).
- [x] `06_GMO_Supporting_Systems/Mode_Init_System.md` → **Keep (verified, header added) 2026-06-13** — play/playbook init (playbooks/play_id migration) doc; **accurate**. Verified `initialize_playbook_settings()` (gameplan_routes.py L678) seeds documented shape with `play_id`-based offense seeding (`SEEDED_OFFENSE_PLAY_IDS`); team play copies (team_manager.py ~L636-666) carry `play_focus`/`target_shooter`/per-team `effectiveness`/`momentum`/`cloaking` + stats containers. **Fixed:** added verification header (noted code-only extras `even_distribution_all` + `_meta.schema_version:2` absent from the doc's shape). **Init-family consolidation (per user):** designated Mode_Init the **owner** of the `initialize_playbook_settings` shape + team play-copy fields; enriched it with the concrete seeded defaults (fast_breaks 33/33/34, man_defense 100/0/0, zone split, SEEDED_OFFENSE_PLAY_IDS); added reciprocal "Related init docs" cross-links across all four init docs (Mode_Init, Season_Init, + already-swept `05_GP/Game_Init_System.md` & `05_GP/Computer_Team_Game_Init_System.md`); trimmed the duplicated `initialize_playbook_settings`/play-copy descriptions in Computer_Team_Game_Init + Season_Init down to pointers. (Season_Init full review still pending its own turn.)
- [x] `06_GMO_Supporting_Systems/Offense_Plays_System.md` → **Keep (verified, header + 4 path fixes) 2026-06-13** — play_id-migration overview (universal vs team-owned play model, selection/persistence, details routing, training/reporting); **accurate**. Verified set-play aliases `SET_PLAY_POSITION_ALIASES=("target_shooter","pos1","pos2","pos3","pos4")` (playbook_weights_utils.py L24), endpoints `/api/play/{play_name}` (L154) + `/api/plays/{play_id}` (L187), `plays_effectiveness_changes` (training_execution_v2.py ~L149). **Fixed:** added verification header; repointed all 4 stale `docs/docs_1_systems/...` Related Docs paths to correct relative locations (HCO + Sim_Playcalling → `../05_GP_`, Plays_Page → `../00_General_`) — targets confirmed to exist. **Absorbed `Play_Builder_System.md`** (per user) as a new "Play Builder (authoring)" section + removed the now-self Related-Docs link.
- [x] `06_GMO_Supporting_Systems/Play_Builder_System.md` → **DELETED (merged) 2026-06-13** — per user decision, folded into `Offense_Plays_System.md` as a new "Play Builder (authoring)" section (motion/set structure, skeleton variants, builder routes, name-based upsert), deduping the shared universal-play-doc-shape / set-play-aliases / routes that both docs carried. Verified all builder routes (play_routes.py L59/134/154/187/212), 4 set-play variants, `play-builder-v2.html`, `base_loop`. Repointed the 2 external referrers (`Plays_Page_System.md`, `O_&_D_Plays_Collections.md`) to Offense_Plays. No remaining references.
- [x] `06_GMO_Supporting_Systems/Playbook_Likely_Shot_Percentage_System.md` (renamed by user from `Playbook_Weights_System.md`) → **Merged → Playbooks_Page.md + DELETED 2026-06-13** — full content folded into Playbooks_Page.md as the "Likely Shot Percentages (Derived Output)" section (headings demoted one level) per user decision (the readout is purely derived from the Playbooks %/PCC config documented there). Algorithm was verified 100% accurate before merge (60/40 split L197/L201, target-shooter priority, pos1-4/skeleton fallback, even Playcall split 100/N, floor+largest-remainder rounding, `WEIGHTS_ALGORITHM_VERSION=1`, `NON_SUCCESS_VARIANTS`, `weights_cache_is_stale`; endpoints `GET /api/playbooks` L1813 + `POST /api/playbooks` L2964). Standalone file deleted.
- [x] `06_GMO_Supporting_Systems/Playbooks_Page.md` → **Keep (verified, header + owner xref + absorbed Likely-Shot doc) 2026-06-13** — Playbooks page persistence/normalization + franchise two-stage (FTD↔game-doc) policy; **accurate**. Verified canonical shape + `fast_breaks` defaults (triangle 34/rim_runner 33/covert_release 33) + defense maps (zone_23/32/131, man_normal/pressure/loose) match `initialize_playbook_settings`; normalization `build_simplified_playbook_settings` (playbook_settings_utils.py); `slot_assignments` still a compatibility output (pc_order authoritative). **Fixed:** added verification header + ownership pointers (canonical shape → Mode_Init); added Playcall-Center config-vs-runtime cross-link (→ Playcall_Center.md / Sim_Playcalling_System.md); **absorbed the full Playbook_Likely_Shot_Percentage_System.md** as a new derived-output section.
- [x] `06_GMO_Supporting_Systems/Player_Attribute_System.md` → **Keep (verified + corrected) 2026-06-13** — frontend display-order/formatting doc. Emoji map (`getEmotionEmoji` L1103-1109) + momentum-pill math (`createMomentumPill` L1112+, `(mo/10)*50%`) + NG `toFixed(2)` all match exactly. **Fixed:** the doc claimed `ATTRIBUTE_ORDER` was 15 attrs incl. MO at L16-18 — actually **14 attrs (MO excluded)** at L53-55 (code comment L52 confirms exclusion); MO is shown on the lineup screen (`set-lineup.js` L1528/L1568), not the Training Report grid. Updated code block, all "15 attributes" mentions, filtering-logic line refs (271-284→~937-951), and added verification header.
- [x] `06_GMO_Supporting_Systems/Position_Ratings_System.md` → **Keep (verified + minor corrections) 2026-06-13** — **every weight table matches `position_ratings.py` exactly** (PG/SG/SF/PF/C, recruit PF/C 30+height10, short-height <71 tables 35+height0, threshold `RECRUIT_SHORT_HEIGHT_THRESHOLD_IN=71.0`, height map `1+(h-60)*(99/24)`). Persisted DB field `position_ratings` confirmed (game_manager L105). **Fixed:** clamp is `_clamp(total, lower=1, upper=None)` = floored at 1, **no hard upper cap** (doc said "clamped to 1–100"); corrected `_clamp` signature (`upper: int|None=100`) + `compute_position_ratings` default profile; noted `add_position_ratings` writes in-memory key `ratings` vs canonical `position_ratings`. Added verification header.
- [x] `06_GMO_Supporting_Systems/Practice_Squad_System.md` → **Keep (verified + line-ref refresh) 2026-06-13** — NOT a stub (213 lines, thorough). Substance **fully accurate**: 12+3 roster invariants, TS-vs-`players` separation, lifecycle, CH-gated progression bands (`_ts_progression_band` >79→(-1,4)/>59→(-1,3)/>39→(-2,3)/>19→(-2,2)/else(-3,2) — exact), week-35 CPU cut odds (RT<10→100%/<15→50%/<20→25% over active+TS — exact), 13 evolving attrs, fold-back capacity. `api.py` anchors (5304/5491/5530) still correct. **Fixed:** ~19 drifted line refs across `franchise_routes.py` (4038→4087, 8153→8267, 8177→8291, 8239→8353, 8263→8377, 8647→9123, 9027→9520, 9372→9865, 9455→9948, 9493→9986, 9533→10026, 9569→10062, 11666→12160), `franchise_manager.py` (155→182, 335→380, 580→617), `franchise-command-center.js` (2178→2247, 3518→3659, 3811→3917). Added verification header.
- [x] `06_GMO_Supporting_Systems/Press_Conference_System.md` → **Keep (verified, header only) 2026-06-13** — PGPC flow doc; **fully accurate, no corrections**. Verified `FRANCHISE_PGPC_AT_EOG_ENABLED=false` default (gameCompletionPopup.js L12), `PGPC_MIN_WEEK_FOR_PROGRAM_NARRATIVE_QUESTIONS=5`, selection `randint(6,8)` weighted + ≤3 `always` (`select_pgpc_questions_for_session`), `shuffle_answers_for_display` first-four-A–D, API routes (`press_conference_routes.py`), all referenced modules + `docs/To Do/PGPC_Snapshot_Schema.md` exist. References symbols by name (no line drift). **Fixed:** added verification header only.
- [x] `06_GMO_Supporting_Systems/Rank_Prestige_System.md` → **Keep (verified + symbol/path fixes) 2026-06-13** — prestige/national-rank v2 spec; **fully implemented, every formula matches `franchise_rank_prestige.py` exactly** (delta tiers 8/6·9/8·10/10·13/12·18/15, wk1-4 2× multiplier, floor/ceiling dampeners, ranking-score phase taper, `sos_score=(129-sos_avg)*4`, constants VERSION=2/SOS_DEFAULT=64/FLOOR=200/CEIL=800). Wired into `franchise_routes.py` (SOS accumulators `sos_rank_sum`/`sos_games_played`, v2 gate `use_franchise_rank_prestige_v2`, idempotency `rank_prestige_last_applied_week`). **Fixed:** added verification header clarifying pseudocode vs real symbol names (ranking fn is `rank_teams_for_week`, not `generate_national_rankings`; `calculate_prestige_delta`/`calculate_ranking_score` real signatures); repointed stale `docs/docs_1_systems/...` self-ref + enriched Key Files.
- [x] `06_GMO_Supporting_Systems/Recruiting_System.md` → **Keep (verified, minor fixes) 2026-06-13** — large hybrid spec/impl doc; **core code logic ~100% accurate**. Verified `RecruitManager` (`generate_recruits_list` default 40 / season-init passes 300 at `franchise_manager.py:430` & `franchise_routes.py:12332`, `_select_archetype`, `_generate_recruit_profile(archetype, year)`, `_generate_weight`, `YEAR_TIER_RANGES`), `FranchiseManager._build_recruit_lean`; complete-week lean fns (`_apply_performance_based_recruiting_lean_updates` L7432, `_apply_complete_week_recruiting_lean_updates` L7626, both idempotency maps); RT buckets (`getRtBucketClass` 0-40/41-60/61-80/81+, `getRecruitRtBucketClass` JH-only 0-29/30-39/40-49/50+, `getRecruitRtBucketClassForYear`); `randomize_game_attributes` quirk (NG=1.0/MO=0/CH&EM=randint(1,100), overwrites archetype CH). **All tables match code exactly** (archetype weights, 4 year-tier ranges, 20 archetype configs, weight-by-height bands). All frontend files + `tests/test_recruit_archetypes.py` exist. **Fixed:** added title/verification header (file had none); `_generate_recruit_profile(archetype)` → `(archetype, year="JH")`; noted `generate_recruits_list` default=40 vs season-init 300; added `getRecruitRtBucketClassForYear` year-switch wrapper.
- [x] `06_GMO_Supporting_Systems/Season_Init_System.md` → **Keep (verified, header) 2026-06-13** — new-season rollover (persist/reset) doc; **accurate**. Coaching-focus carryover verified: `carryover_coaching_focus_counts_for_new_season` (ratio 0.25 = 75% reduction, int-rounded, 4 archetypes) called by `finish_season` (franchise_routes.py:12264); week-1 camp weight `random.randint(2,4)` else 1 confirmed. Content intentionally high-level + drift-resistant. (Earlier pass already trimmed play-copy duplication → `Mode_Init_System.md` + added init-family cross-links.) **Fixed:** added verification header.
- [x] `06_GMO_Supporting_Systems/Team_Attribute_System.md` → **Keep (verified + line-ref refresh) 2026-06-13** — large (468-line) faucet/sink doc; **substance accurate**. `init_team_attributes` mode ranges match `team_manager.py` exactly (franchise attr(-1,1)/chem7-10/rebound0.2/shot100-120; single-tourn attr(-10,10)/chem7-25/rebound0.0-0.4). EOG tables spot-checked in `update_team_attributes_after_game` (franchise_routes L1287-1398): chemistry win/loss bands, shot_threshold FG% 50/45 thresholds, discipline +1..+2, rebound-down −0.05..−0.10, distant-sim override — all exact. **Fixed:** stale "January 2025 ✅ COMPLETE" header → verification header; all drifted Key Files line refs (init_team_attributes 185-226→463, ensure_team_objects_exist 152-299→993, load_team_attributes_from_doc 196-244→575, run_franchise_training 1045-1061→10536, _apply_team_training_points 685-726→1305); tagged Single Game/Tournament mode ranges **(sunset)**.
- [x] `06_GMO_Supporting_Systems/Tournament_Execution_System.md` → **Keep (verified, header only) 2026-06-13** — consolidated franchise-EOS + shared-bracket reference; **fully accurate, no corrections**. Verified EOS week constants (CONF (27,28,29)/REGION (30,31)/NATIONAL (32,33,34)), bracket_engine (`generate_bracket` 1v8/4v5/2v7/3v6 + matchup shape, `get_round_name`/`save_game_result`/`advance_bracket`), region/national init+reconcile+`get_eos_week_games`, all invariant/heal helpers (`_resolve_user_eos_game_meta_or_raise`, `_stamp_eos_meta_on_game_doc`, `_eos_heal_all_eos_from_games`, `_complete_week_finish_cpu_and_persist`, `_maybe_reconcile_region_for_eos`, `merge_phase_a_eos_blobs...`), all 7 test files + both §13 refs. Symbol-name-based (drift-resistant). **Fixed:** added verification header only. (Companion to already-verified EOS_Write_Path_Inventory + Franchise_Tournament_System.)
- [x] `06_GMO_Supporting_Systems/Training_Notes_System.md` → **Merged → Training_System.md + DELETED 2026-06-13** — per user decision; thin (28-line) play_id-migration note fully redundant with Training_System's existing Training Notes Section. Folded the one unique nugget (notes display play `name`, matching may use `play_id`, reporting-not-persistence layer) into that section. Repointed 3 code-comment refs to the doc (`training_notes.py:2`, `training-report.js:1807`, `training_execution_v2.py:169`) → `Training_System.md`. Standalone deleted.
- [x] `06_GMO_Supporting_Systems/Training_System.md` → **Keep (verified + absorbed notes doc) 2026-06-13** — master training doc (795 lines); **accurate & well-maintained**. Spot-checks pass: training points 30/24 (`get_training_points` L10489/L10516/L10637), split endpoints user L10556 / distant-cpu L10580, `parse_coaching_focus` (L342), focus mult `[1.5,1.6,1.7,1.8]` (7 sites), training-camp CH bonus scale `_training_camp_bonus_range_for_ch` (>80(4,10)/>60(3,8)/>40(2,6)/>20(1,4)) + per-position core attrs exact, 0.25 season carryover. **Merge decisions:** (1) `Coaching_Focus_Implementation_Map.md` → **kept separate** (code-truth status matrix; tightened reciprocal cross-link from the Coaching Focus Amplifiers section); (2) `Training_Notes_System.md` → **merged + deleted** (nugget folded into Training Notes Section). **Fixed:** added verification header + both cross-link/merge changes.
- [x] `06_GMO_Supporting_Systems/Training_System_Live_Feed.md` → **Keep (verified, 1 fix) 2026-06-13** — **highly accurate**. Verified vs `training_loading_highlights.py`: `_MAX_LINES=36`, `_GENERIC_FRACTION=0.25`, session/lead/combo `0.70/0.15/0.15`, no-combo 70/30, combo gap `second>third+3` (`_combo_archetype_from_ftd`), lead = max FTD counter w/ `random.choice` tie-break, freshman ±4 vs default ±3 strict band (`_player_attr_delta_qualifies_for_feed`), one `COACHING_FOCUS_FLAVOR`/build, breaks/scrimmages `>0` gates, shot_threshold/rebound_modifier excluded. Copy lives in `training_feed_lines.py` (imported pools confirmed). **Fixed:** repointed loose team-drill keys (`offense_efficiency`, `P_T`, …) → canonical `_TEAM_ATTR_POOLS` keys (`offensive_efficiency`, `defensive_efficiency`, `fb_efficiency`, `pt_efficiency`, `fb_opp_modifier`, `pt_opp_modifier`) + noted team attrs use the same strict ±3 dead band.

## 07_Design_Systems (6)

- [x] `07_Design_Systems/Attribute Tour Mechanic (standalone).html` → **DELETED 2026-06-13** — self-unpacking interactive mockup bundle (~600KB). **Correction:** the feature **did** ship (FTE v2 Attribute Tour — `js/shared/attributeTour.js`, `css/attribute-tour.css`); the prototype was the design-of-record. Per user decision, deleted the mockup; live implementation is now the reference. Code comments name the *feature* (not the file), so they remain valid.
- [x] `07_Design_Systems/FTE Onboarding Redesign.html` → **DELETED 2026-06-13** — interactive mockup bundle. **Correction:** the redesign **did** ship (FTE v2 funnel — `tutorial-situation.js` et al.); it was linked from `fte_system.md` L388. Per user decision, deleted the mockup, repointed the `fte_system.md` "Visual reference / design notes" row to the live implementation. Code comment in `tutorial-situation.js` names the redesign initiative (not the file), left as-is.
- [x] `07_Design_Systems/Resource_Page_Design_System.md` → **Keep (verified, 2 fixes) 2026-06-13** — design spec; **token values accurate** vs reference impl `stats.css`/`stats.html` (all colors `#0d0f14`/`#141720`/`#1c2030`/`#252a3a`/`#e8903a`/`#4a9eff`/`#e8eaf0`/`#6b7280`/`#34d399`/`#f5c542`, Barlow/Barlow Condensed, section-label `0.2em`, back-button `clip-path` exact, card grid `repeat(auto-fill, minmax(280px,1fr))`). **Fixed:** layout `max-width 1400px → 1200px` + padding `32px → 20px` to match `.resource-page-container`; added header noting doc→code CSS-var name mapping (`--surface`→`--bg-elevated`, `--surface2`→`--bg-hover`, `--accent2`→`--group-blue`, `--muted`→`--text-muted`; values identical). **2026-06-13 (b):** user asked whether to roll this into `Styleguide.md` → decided **keep separate** (the two systems *conflict* on fonts + every accent hex + table system — Barlow/teal vs Bebas-Inter/canonical-grid; merging would make the styleguide self-contradictory). Added **reciprocal divergence flags** in both docs and noted the unresolved design question (intentional sub-brand vs. reconcile to canonical).
- [x] `07_Design_Systems/Sonic_Identity.md` → **Keep (reviewed, headings fixed) 2026-06-13** — pure creative audio north-star brief; **no code-coupled claims** to verify (nothing to drift). Content kept as-is. **Fixed:** added a top-level `# GOB Sonic Identity` H1 + status note; repaired 11 malformed `##Heading` (no-space) section headers so they render as headings.
- [x] `07_Design_Systems/Soundtrack_System.md` → **Keep (verified, no changes) 2026-06-13** — **100% accurate** vs `musicController.js`. Confirmed all constants/tracks (`scouting-track-1/2.mp3`, `arcade-pulse-1.mp3`, `pixel-pulse-1.mp3`, `Timeout_Loop.wav`, `CRUNCH_TIME_SECONDS=121`, `CRUNCH_SCORE_DIFF=6`, `DEFAULT_VOLUME=0.4`, `gob_scouting_ambience_enabled`, `preload="none"`), the `evaluateGameplayTrack` switching logic, and all referenced exported functions. Added verification header.
- [x] `07_Design_Systems/Styleguide.md` → **Combined + deleted 2026-06-12** — pre-redesign styleguide (navy `#27408E` shell, ellipse texture, plain Bebas Neue) superseded by `00_General_Systems/Styleguide_updated.md`; its three unique sections (Semantic/Data Colors, Tutorial Alert Coach Card, Tutorial Alert Resume Footer) folded into the updated doc. Live code comments repointed (`common.js`, `css/gob-tutorial.css`); archived references in Z-Completed left as-is.

## Root files (2) — ❌ OUT OF SCOPE (not reviewing)

- [ ] `ENV_VARIABLES.md` →
- [ ] `SECURITY_BASELINE.md` →

## tasks (1) — ❌ OUT OF SCOPE (not reviewing)

- [ ] `tasks/Defense_ID_Migration.md` →

## projects (26) — ❌ OUT OF SCOPE (not reviewing; incl. Website_Copy, secondary_announce)

- [x] `projects/screen_capture_tool_spec.md` → **Archived 2026-07-05** — manual capture shipped (`capture*.js`); auto-capture moments deferred to `bugs.md` §Screen capture.
- [ ] `projects/Animation_Cleanup.md` →
- [x] `projects/Animation_System_Updated.md` → **Deleted 2026-07-05** — was a redirect stub; all code comments repointed to `05_UESS_System/UESS_System.md` (schema §3, coord sync §9, AG §9.3), `Step_By_Step_System.md` §HCT, and `Rebound_System.md` §DREB.
- [x] `projects/animation_wonkiness.md` → **Deleted 2026-07-05** — open buckets 1–2 merged into `projects/bugs.md` §Animation timing pauses; fixed buckets 3–4 summarized there.
- [x] `projects/Distant_Sim_Tuning.md` → **Archived 2026-07-05** to `Z-Completed/` — calibration targets, playtest checklist, and live MC results merged into `04_Franchise_Mode_Systems/Distant_Game_Sim_System.md`; `distant_sim_monte_carlo.py --write-doc` repointed.
- [x] `projects/Dynamic_FCP_Brief.md`, `Dynamic_HCO_Motion_Brief.md`, `Dynamic_HCO_SP_Brief.md` → **Archived 2026-07-05** to `Z-Completed/` (HCT brief pattern). Canonical: `FCP_System.md`, `Dynamic_HCO_System.md` (motion + set plays merged). Code comments repointed.
- [x] `Dynamic_HCO_SP_System.md` merged into `Dynamic_HCO_System.md` (§ Set plays) — July 2026.
- [ ] `projects/Dynamic_HCT_Turns.md` →
- [ ] `projects/News_System.md` →
- [x] `projects/Recruit_Generation_System.md` → **Archived 2026-07-05** — obsolete scratch; superseded by `Practice_Squad_System.md` + `Recruiting_System.md`.
- [x] `projects/Micro_Movements_Brief.md` → **Archived 2026-07-05** — superseded by `Shot_Micro_Movements_System.md`.
- [x] `projects/secondary_announce/*` → **Archived 2026-07-05** to `Z-Completed/` (brief + HTML); shipped — see `Announcement_System.md`.
- [ ] `projects/Resend_Project_Brief.md` →
- [ ] `projects/Resend_Project_Work_Plan.md` →
- [ ] `projects/Social_Activation.md` →
- [ ] `projects/Tutorial_Alerts_System.md` →
- [ ] `projects/bugs.md` →
- [ ] `projects/cloudflare_migration.md` →
- [ ] `projects/offensive_state_hardening.md` →
- [x] `projects/rebounding_logic.md` → **Deleted 2026-06-21** — superseded by `06_Gameplay_Systems/Rebound_System.md`, which now owns the implemented geo-helper, all-eligible rebound scoring, fallback, tie-breaker, and animation contracts.
- [ ] `projects/simulate_quarter_api_cleanup.md` →
- [ ] `projects/staging_otps.md` →
- [ ] `projects/universal_3point_helper.md` →
- [ ] `projects/used_otp_codes.md` →
- [ ] `projects/user_feedback_form.md` →
- [ ] `projects/Website_Copy/alpha-box-copy.md` →
- [ ] `projects/Website_Copy/alpha_mode_select_copy.md` →
- [ ] `projects/Website_Copy/carousel_copy.md` →
- [ ] `projects/Website_Copy/faqs.md` →
- [ ] `projects/Website_Copy/fte-copy.md` →
- [ ] `projects/Website_Copy/tutorials_copy.md` →

## projects/Z-Completed (14) — ❌ OUT OF SCOPE (not reviewing)

- [ ] `projects/Z-Completed/Championship Announce.html` →
- [ ] `projects/Z-Completed/Fast_Break_Refactor.md` →
- [ ] `projects/Z-Completed/Movement_Rate_Refactor.md` →
- [ ] `projects/Z-Completed/Parallel_Franchise_CPU_Sims_and_Week_Finalization_Work_Plan.md` →
- [ ] `projects/Z-Completed/Playbooks_Tutorial_Brief.md` →
- [ ] `projects/Z-Completed/README.md` →
- [ ] `projects/Z-Completed/SFX_Brief.md` →
- [ ] `projects/Z-Completed/SFX_Manager_Implementation.md` →
- [ ] `projects/Z-Completed/Scouting Link Options.html` →
- [ ] `projects/Z-Completed/Scouting_Tutorial_Brief.md` →
- [ ] `projects/Z-Completed/Sound_Design_Update.md` →
- [ ] `projects/Z-Completed/Tutorial Alert Modal.html` →
- [ ] `projects/Z-Completed/eligible_players_bug.md` →
- [ ] `projects/Z-Completed/training-tutorial-brief.md` →

## projects/scouting-tutorial-reference-files (7) — ❌ OUT OF SCOPE (not reviewing)

- [ ] `projects/scouting-tutorial-reference-files/Scouting.html` →
- [ ] `projects/scouting-tutorial-reference-files/assets/gob-nav.js` →
- [ ] `projects/scouting-tutorial-reference-files/assets/gob-tutorial.css` →
- [ ] `projects/scouting-tutorial-reference-files/assets/scouting/play-usage.png` →
- [ ] `projects/scouting-tutorial-reference-files/assets/scouting/roster-attributes.png` →
- [ ] `projects/scouting-tutorial-reference-files/assets/scouting/season-stats.png` →
- [ ] `projects/scouting-tutorial-reference-files/assets/scouting/starting-five.png` →

---

*`projects/_documentation_sweep.md` (this file) is excluded from the review count.*
