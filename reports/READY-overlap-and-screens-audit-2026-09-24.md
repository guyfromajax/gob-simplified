READY overlap-and-screens audit reviewed 2026-09-23T22:30:29Z — audit only, nothing built. A1 CONFIRMED: sprite is 5.25 grid units wide at median height; EVERY posture distance (deny 2.0, on-ball 2.5/3.5/4.5) is smaller than one sprite, and zone_sink.MIN_SEPARATION 2.0 was calibrated off the engine's own p5, never off the renderer. Overlap is broad not local: 74.8% of steps have a sub-3.0 pair, 14.5% have an exact coincidence, no builder owns it. KEY DESIGN NUMBER: a flat 3.0 rule moves 25.3% of placements and 28.6% of what it separates is deliberate deny/help work — so the pass must be ASSIGNMENT-AWARE. Screens confirmed non-spatial AND are an active cause: the screener is sent to the SAME named location as the man he screens for on 90.5% of screens, which is why O-C/O-PF is the largest exact-coincidence pair. SCR_S is just shot_made; both stats are DISPLAY-ONLY, nothing feeds development or awards, so the sunset is cheap. Switch needs an override layer read by 18 consumer sites across 5 modules; the man matchup map is never mutated mid-possession today (zone overlap resolution is the precedent). Phases: 1 separation pass (medium), 2 spatial screens + switch (large), 3 stats (small).
