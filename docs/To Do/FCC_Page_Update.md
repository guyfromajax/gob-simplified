

**Standings tab**
Show user team conference standings on top, show region sister conference standings on the bottom

**Roster tab**
Leave as is

**Team tab**
Leave as is

**Stats tab**
Remove this from the FCC and replicate the exact tab display/functionality on the new stats.html screen

**Schedule tab**
Keep as is

**Team Traits tab**
Remove this from the FCC and replicate the exact tab display/functionality on the new team-traits.html screen

**Recruits tab**
Keep as is

**Tournament tab**
Keep as is

**Rankings tab**
Remove this from the FCC and replicate the exact tab display/functionality on the new rankings.html screen


**Resources** (new tab) (make all buttons blue fill with white bold copy, same as the current Game Plan button on the FCC -- layout buttons in a 3-column grid, filling horizontal rows first, from right to left)
-"Standings" button: takes user to standings.html page
    -land on the user team conference and sister conference standings
    -have toggles for all 16 conferences
    -note this is simply replicating our current previous version of the Standings tab that we are updating
-"Stats" button: takes user to stats.html page
    -land on the user team conference stats
    -have toggles for Conference, Region, National
    -note this is simply replicating our current previous version of the Stats tab that we are removing from the FCC
-"Schedule" button: takes user to schedule.html page
    -land on the user team conference schedule
    -have toggles for all 16 conferences
-"Team Traits" button: takes user to team-traits.html page
    -land on the user team conference team traits
    -have toggles for Conference, Region, National
    -note this is simply replicating our current previous version of the Team Traits tab that we are removing from the FCC
-"Rankings" button: takes user to rankings.html page
    -land on the Top 25 rankings
    -have toggles for Top 25 / All 128
    -note this is simply replicating our current previous version of the Rankings tab that we are removing from the FCC
