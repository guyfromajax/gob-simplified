# Netlify Configuration for GOB Simplified

[build]
  publish = "FrontEnd/static"
  # No build command needed (static site)

# Redirect root to mode-select.html for easier tournament/franchise mode testing
[[redirects]]
  from = "/"
  to = "/mode-select.html"
  status = 200
  force = true

